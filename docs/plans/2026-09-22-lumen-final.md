# Lumen Card — ФИНАЛЬНЫЙ план до готовой версии (2026-09-22)

> Этот документ заменяет очередь задач из планов фаз 5 и 6. Новых задач в него не добавлять: всё, что придёт после, — либо дефект (чинится в разделе A), либо откладывается за релиз (раздел D). Ведётся по `superpowers:subagent-driven-development`; ревью — одно на волну.

## Что значит «готово»

Версия считается готовой, когда одновременно верно всё:

1. На телевизоре пользователя (Philips 50PUS8057, режим движения `lite` — его выбор) нет ни одного известного дефекта уровня «важно»: кадр героя виден и меняется, логотип не подменяется на ходу, рулетка крутится, карточка длинного сериала открывается без провала, меню и фокус читаются.
2. По HUD с телевизора: главная в покое ≥ 55 fps, при листании ≥ 40, карточка «Дораэмон» без провала ниже 20.
3. Каждый пункт из четырёх отзывов пользователя (интервью 2026-09-21, отзыв 2026-09-21 вечером, скрины 2026-09-22 ×2) либо закрыт, либо явно отклонён им самим.
4. Все тесты зелёные, сборка актуальна, ES5 ok, планы и README отмечены.

Публикация в `main` — **решение пользователя**, план его не принимает.

---

## Состояние (обновляется; последняя правка 2026-09-24, после контрольного ревью четвёртого раунда и пятого фикс-раунда)

Ветка `feat/lumen-v2`, последний коммит кода — `49899e9` (пятый фикс-раунд); после него — только коммит документов этого раунда. **2114 тестов, 0 падений** (`node --test "test/*.test.mjs"`), `dist` актуален, ES5 ok, `manifest.json` актуален (150 подборок). Версия в баннере — `0.2.0` (C5 не делался).

**Публикация.** `feat/lumen-v2` — ветка публикации, а не черновик: загрузчик `lumen.js` подключает `dist/lumen_card.js` рядом с собой (Pages или jsDelivr `@feat/lumen-v2`) с часовой меткой в адресе, каталог подборок `LC.MANIFEST_URL` берётся с Pages той же ветки. Поэтому каждый push этой ветки доезжает до телевизора пользователя: код — не позже чем через час (метка меняется раз в час), каталог — до 12 часов (кэш каталога в плагине). Push делает координатор. Третий фикс-раунд запушен целиком: `de0e2c8` (его коммит документов) — 2026-09-23 22:13. Четвёртый фикс-раунд запушен целиком: `0c79048` (его коммит документов). Коммиты пятого фикс-раунда (`a67811c`…`49899e9` и коммит документов) на момент этой правки **не запушены** — их push за координатором. `main` не трогается — это решение пользователя. Кэш CDN обходится часовой меткой в загрузчике. Раздел A (волна A) закрыт целиком. Незакрытым остаётся раздел B: снимков с телевизора после фазы 6 не было, поэтому критерии 1 и 2 «готово» не подтверждены — все волны ниже проверены только на стенде.

| сделано | закрыто |
|---|---|
| Фаза 5, волны 1–6 (Task 47–64, 53, 54, 50c, 44, 60, 62, 63) | 2026-09-21 |
| Фаза 6 волна: 67 (серии окном), 70 (кадр под рядами), 72 (рулетка в lite), 71 (логотип), 68 (HUD), 73 (пресет на экраны) + фикс-раунд по ревью | 2026-09-22 |
| Загрузчик: часовая метка против кэша CDN на 7 дней | 2026-09-22 |
| Волна A: дефекты A1–A6 + A7 (найден по ходу) + фикс-раунд по ревью | 2026-09-22 |
| Волны после `8074aba` — разбор композиции, постеры, инвентаризация, долги, два дефекта (по строке на коммит ниже) | 2026-09-22 … 2026-09-23 |
| Волна «скрины с ПК» `95edc3c`…`8c60da8` и последняя волна перед ревью `363bc8d`…`d0738c0` (по строке на коммит ниже) | 2026-09-23 |
| Независимое ревью (три ревьюера, 27 находок, блокеров нет) и три фикс-раунда `77f2264`…`ce2df47` — раздел «Независимое ревью» ниже | 2026-09-23 |
| Контрольное ревью `84c7b27..de0e2c8` (5 находок, блокеров нет) и четвёртый фикс-раунд `5e70941`…`65dc9b8` — раздел «Контрольное ревью» ниже | 2026-09-24 |
| Контрольное ревью четвёртого раунда `de0e2c8..0c79048` (6 находок, блокеров нет) и пятый фикс-раунд `a67811c`…`49899e9` — раздел «Пятый фикс-раунд» ниже, не запушен | 2026-09-24 |
| Снято по данным: Task 45 (калибровать нечем — `lite` на телевизоре поставил пользователь сам, см. Task 74 в плане фазы 6), Task 66 (влит в 68), Task 74 (lite выбран вручную), чипы настроения отдельным рядом (нет 32 px высоты) | — |

### Что сделано после `8074aba`

Всё по `git log --reverse 8074aba..5da8ae6`; SHA и формулировки — из сообщений коммитов, подробности (замеры, отвергнутые варианты) — в их телах.

**Закрытие волны A и финального ревью (2026-09-22)**
- `2ecca5c` docs: отметки о выполнении по фазе 6 и волне A.
- `bed9150` фикс-раунд финального ревью — оговорка масштаба, комментарии, README (вместе с правкой `src/30_css.js`, `src/62_badges.js`, `src/80_settings.js` и тестом).
- `e68a67a` docs: два долга из финального ревью — ширина плиток хаба и тонкие клетки бюджета.
- `375c38c` fix(hero): название на главной выводится один раз — текстом или логотипом (отзыв пользователя «переключение названий есть»).

**Постеры и картинки**
- `49b74ca` fix(hub): ширина плитки и карточки сетки — из той же цепочки, что раскладка (закрывает долг раздела D).
- `ee0b54c` fix(images): у постера свой допуск апскейла — 1.111 вместо 1.176.
- `9480e0e` fix(poster): постер карточки прижат к верху ячейки — голова не срезается.
- `6bcdba0` feat(posters): настройка источника постера — три положения, дефолт прежний.

**Разбор композиции дизайнера** (`d550f93` — сам разбор, `docs/design/2026-09-22-composition-review.md`; что из него сделано — таблица в его конце)
- Первые четыре правки:
  - `6d466bf` fix(hero): название первым и крупнее, мета под ним и мельче (пп. 1.1, 1.2);
  - `5601edb` fix(edge): кромка режет картинку, но не текст — четыре экрана (п. 6; замер той правки снят при DPR 1 — поправлено в `657ffa8`);
  - `7e56855` feat(card): затемнение под содержимым карточки — один линейный градиент (п. 4.1);
  - `44a079a` feat(roulette): барабан показывает выборку до вращения (п. 5.1).
- Второй эшелон:
  - `10003ae` feat(episodes): кадр серии 16:9 вместо полоски — одна раскладка на оба вида (п. 3.1);
  - `73a479f` fix(edge): кромка не режет название серии — срезанная плитка показывает кадр;
  - `4d090c2` feat(card): двухуровневое название вместо обрезки многоточием (п. 2.1);
  - `5191949` fix(card): режиссёр уходит из мета-строки — он дублируется лентой людей (п. 2.2);
  - `6de25af` feat(card): «Режиссер» и «Актеры» — одна лента людей (п. 4.2);
  - `c51d3dd` feat(card): чипы карточки — одна схема, оранжевое у оценки, читаемость на кадре (п. 2.3);
  - `5f589c6` fix(roulette): управление собрано слева, вниз с подборок доводит до «Крутить» (п. 5.2).
- Косметика:
  - `255f911` fix(hero): затемнение кадра — подушкой под текстом, а не третью кадра (пп. 1.3, 1.4);
  - `0378a1c` fix(card): плитка «добавить комментарий» — сплошная, как карточки рядом (п. 4.4);
  - `6d3c2c7` fix(rows): дубль «Ещё» уходит из шапки ряда; инвариант назван по состоянию (п. 1.5);
  - `e3189e6` docs(rows): точнее про плитку «Ещё» — её дописывает onScroll, а не разметка.

**Волна из инвентаризации (2026-09-23)**
- `06e3a6d` docs(settings): «Кадр над рядами: выключен» называет переход, который гаснет вместе с ним.
- `1f1dd49` fix(roulette): подсказка под «Крутить» убрана — экран объясняет себя сам (п. 5.3 разбора).
- `8fc2e8b` feat(manifest): сезонные подборки к 9 мая и 14 февраля.
- `a90d045` feat(roulette): входы из шапки хаба и из сетки подборки (долг Task 23).
- `e44c543` docs(card): разница по высоте между фильмом и сериалом — задумана, объяснена у правила (п. 2.6 разбора).
- `afaeafe` feat(card): логотип названия в карточке — один вывод, место по пропорции, общая с героем механика (долг Task 24).
- `da93832` docs(readme): логотип в карточке, входы в рулетку, сезонные подборки, «Кадр над рядами».

**Волна долгов (2026-09-23)**
- `657ffa8` fix(edge): шапка карточки занимает первый экран — описание не встаёт под кромку ни при какой высоте шапки.
- `dc13bbd` fix(layout): пороги раскладки считают экран с полом кегля Lampa 10.6 px (закрывает долг раздела D).
- `4cb66b5` fix(personal): «Потому что вы смотрели» и «Новые серии» читают историю на каждой сборке главной (первая половина долга фазы 2; вторая — в разделе D).
- `7c7c822` fix(card): страна в мета-строке — на языке интерфейса, а не всегда по-русски (долг фазы 1, п. 3).
- `ebde4bf` fix(hud): счёт слоёв включает два постоянных слоя Task 46 (долг фазы 4).
- `63a8a78` fix: мелочи ревью фазы 3 — битый кадр заставки, жизнь мини-карты, mousemove, строки меток.

**Волна «два дефекта» (2026-09-23)**
- `8ece232` fix(card): ↑ с дальней серии ведёт на кнопки карточки, а не в поиск Lampa (долг фазы 1, п. 2). Замер на стенде 960×540, DPR 2, кегль 11.4055: кнопки кончались на x = 449, и уже у четвёртой плитки (574…744) Navigator наверх цели не находил; теперь ↑ ставит фокус на ближайшую по горизонтали кнопку. Пультом проверено с серий 1, 2, 4, 6, 8 и 46 «Дораэмона» (и на «крупнее», плоский вид, с серии 46); мышью наведение фокус Navigator не двигает (штатно у Lampa), стрелки идут от последнего фокуса пульта.
- `5da8ae6` fix(edge): второй экран карточки — ряд описания страницами, следующий ряд не выше кромки. Правило кромки для второго экрана задано устойчиво: видна одна страница ряда описания (основная / отзывы / «Смотреть по порядку»), ряд занимает ровно остаток экрана. Стенд 960×540, DPR 2, 3 размера × обычный/плоский вид × фильм 1891 / сериал 100088: текста под кромкой нет ни на одном шаге, следующий ряд начинается на 571.8…575.9.
- Коммит документов этой волны: этот раздел, отметки в планах фаз, «Что сделано» в разборе дизайнера, чек-лист §14, README.

**Волна «скрины с ПК» (2026-09-23)** — по `git log 95c1b51..8c60da8`:
- `95edc3c` fix(hero): левая вуаль в сжатом состоянии едет вместе с текстом — нет чёрного прямоугольника (стенд 1600×900@1: плотная полоса 150…450 px при верхе логотипа 340).
- `111bcc9` fix(rows): ряд главной заполнен до правой кромки сразу, а не по докрутке (первая порция ряда поднята до числа видимых карточек: 7 → 8 на 960×540@2, 7 → 9 на 1840×960@1).
- `cadc57e` fix(rows): ряд-огрызок после дедупликации не показывается — «В тренде за неделю» из 4 карточек.
- `76c4a5d` fix(card): число оценки TMDB нейтральным цветом — оранжевого в ленте чипов нет.
- `cca3dec` feat(hero): настройка «Что показывает кадр главной» — кадр и трейлер или несколько кадров.
- `8c60da8` fix(sources): ток-шоу и подкасты не попадают в подборки — запрет жанра Talk у discover/tv.

**Последняя волна перед независимым ревью (2026-09-23)** — по `git log 8c60da8..d0738c0`:
- `363bc8d` fix(edge): следующий ряд главной не выглядывает и на окнах браузера ПК — ряд кончается на кромке на всём диапазоне. Две причины: интервал правила кромки не покрывал 1840×960 (заголовок следующего ряда целиком над кромкой, 931.3…958.2), а зазор меньше отступа Lampa над фокусным рядом пускал подписи предыдущего ряда в область (1600×900@1: 5.4 px внутри). Замеры после — в теле коммита: 1840×960@1 — 960.0, 1600×900@1 — 905.3, 1280×800@1 — 800.1, 1440×900@2 — 900.3, 2560×1080@1 (кадра нет) — 1079.8, телевизор 960×540@2 — 542.7 при низе подписи 514.2, как и до правки.
- `7a0e93e` test(progress): ширина полосы «Продолжить» под тестом — фейковый DOM понимает «>» и голый тег (закрывает долг раздела D).
- `44086b1` refactor(prefs): свои настройки — только через LC.pref; Storage.field остался у настроек самой Lampa (долг фазы 1, п.5; мест было пять, а не четыре — пятое в `src/68_perf.js`).
- `78d0bdb` test(onscreen): «наша карточка на экране» проверяется по состоянию DOM, а не по заглушке ответа — сторож до рефакторинга (зелёный на прежнем `src/`).
- `34c8d94` refactor(onscreen): «наша карточка на экране» — одна функция `LC.util.onScreen` вместо шести записей (долг фазы 1, п.4).
- `b99531c` fix(hero): возврат из карточки не пересобирает героя — те же узлы, тот же кадр, слайдшоу продолжается (долг фазы 2, п.4).
- `c2a6393` fix(manifest): сериалы франшиз по студиям — «Звёздные войны» полностью (35 сериалов вместо двух), у «Гарри Поттера», «Властелина колец» и Marvel Studios — впервые.
- `10289cc` docs(phase4): маска Lampa при «Кадр над рядами: выключен» — оставлена, долг закрыт с доводом.
- `d0738c0` test(fakedom): find() и children() отдают все совпадения, как jQuery, а не первое (долг фазы 1, п.6).
- Коммит документов этой волны: этот раздел, отметки в планах фаз 1–4.

### Решения пользователя, записанные 2026-09-23

- **Следующий ряд на главной снизу не выглядывает.** Дословно: «мне не нравится, когда есть этот выступ, все знают, что внизу есть что-то». Разбор дизайнера (п. 1.6) предлагал обратное — сдвинуть заголовок второго ряда так, чтобы кромка приходилась на постеры, — **отклонено пользователем**. Довод Task 63 за зазор «ради подглядывания» (`docs/plans/2026-09-21-lumen-phase5-tv-fix.md:340`) этим решением **отменён**. То же правило приняли шапка карточки (`657ffa8`) и второй экран карточки (`5da8ae6`). С `363bc8d` правило держится не только на телевизоре, но и в окнах браузера ПК: тест гоняет 72 клетки на 15 окнах (телевизор, 16:9 и 16:10 от 1280 до 2560 CSS px, окно пользователя 1840×960, 1920×969, 21:9) — следующий ряд не выше кромки, подписи предыдущего не в области.
- **Постеры — переключатель трёх режимов, а не выбор за человека** («Как в Lampa» / «Английские» / «Без надписей», `6bcdba0`; дефолт прежний; второй режим до `ddec47a` назывался «Оригинал» — переименован решением координатора, он даёт английскую обложку, а не обложку на языке оригинала). Требование к кадрированию, дословно: «чтобы эти постеры были нормально по экрану расположены, в плане не обрезана голова, лучше туловище, но не голова» (записано и в `9480e0e`: постер прижат к верху ячейки; в `6bcdba0` — фильтр пропорции 0.64…0.75; его асимметрию задают данные — пустая полоса 0.487…0.650 перед «обоями» 0.486, — а не голова: при прижатом к верху постере узкий теряет низ, `ce2df47`).

### Независимое ревью 2026-09-23 и три фикс-раунда

Диапазон ревью — `8074aba..84c7b27`. Три независимых ревьюера: раскладка, логика модулей, данные/настройки/документы. **27 находок, блокеров нет**: у второго ревьюера 6, у третьего 11 (разбор координатора, находки с процентом уверенности), у первого — 10 (остаток от общего числа; его список в третьем раунде не открывался). Все находки разобраны тремя фикс-раундами, каждый — своими коммитами:

- **Ф1 — раскладка** (`77f2264`…`5e5c902`, 9 коммитов): `77f2264` верхняя вуаль под шапкой Lampa в сжатом состоянии; `d4f23ad` подушка под текстом героя идёт за блоком текста; `ca99f3a` добавка полосы чипов к сдвигу сжатия — в кегле текста; `f54d7d8` при выключенных чипах текст не уходит под кромку кадра; `011f046` зазор под шапкой ряда на «крупнее»; `b1735bc` подпись первого ряда в сжатом состоянии на экране при любом отношении сторон; `e8aa12b` спокойный экран рулетки в области при любых настройках; `c0584e9` барабан рулетки, стопка и ряд франшизы кадрируют постер по верху; `5e5c902` высота кадра карточки одним числом, комментарии по факту.
- **Ф2 — логика** (`0f5ea45`…`1dc8f58`, 7 коммитов, находки 1–6): `0f5ea45` es5check ловит `.finally(`; `20b5f25` уход на карточку гасит показ выборки рулетки (pause); `9ca85a6` без кадра над рядами пробник мерит штатную карточку; `44fab64` ответ деталей в запаркованного героя велит довести показ; `593a479` возврат из карточки ставит атмосферу героя заново; `0fbfbb3` HUD: слои видимого экрана и спрятанных — раздельно; `1dc8f58` возврат на рулетку поднимает погашенный показ выборки.
- **Ф3 — данные, настройки, документы** (`b1c5abf`…`ce2df47` и коммит документов, 11 находок, 4 решения координатора, 2 добора логики из Ф2):
  - `b1c5abf` данные: п.5 — реалити с жанром Talk (Top Gear, The Grand Tour, Single's Inferno) остаются, отсечение перенесено в ответ (`talkOnly`: 10767 без 10764), подкаст The Escape Pod по-прежнему отсекается; решение — «Изгой-один» и «Хан Соло» в «Звёздных войнах» (фильмы запросом Lucasfilm И фантастика: 13 фильмов, все — франшиза); п.6 — комментарий к «Кино о любви» исправлен, запрос не менялся.
  - `a2513e6` п.3 — двухуровневое название: «Re:ZERO» и «Mission: Impossible - …» не режутся по двоеточию (выборка 1322 живых названий: 17 отличий, разбор в коммите).
  - `845af09` п.1 — «Английские» постеры на страницах 2+ сетки спрашивают ту же страницу.
  - `8ef31cd` добор Д1 — возврат к карточке ставит атмосферу её фона заново (стенд, «Один дома» → «Дюна» → «Назад»: канвасов частиц у карточки 0 на `845af09`, 1 после).
  - `e06bb16` добор Д2 — поздние ответы `tmdb.get` отсекают сторожа, мёртвые `.clear()` убраны, заглушки тестов приведены к Lampa; у франшизы нашлось два дефекта: ряд рисовался в закрытую карточку, и скелетон ряда на живой Lampa не показывался никогда (с `906261f`).
  - `ddec47a` настройки: режим «Оригинал» → «Английские» (ключ `original` прежний); п.4 — цена и кэш «Постеров карточек» числами; п.7 — правило ряда-огрызка как в коде; п.11 — сторож дефолтов перечисляет новые ключи.
  - `ce2df47` комментарии: п.10 — задача постеров в коде зовётся «Постеры», а не вторым «Task 74» (50 упоминаний); п.8 и п.9 в коде.
  - коммит документов: п.2 (правда о публикации — выше), п.7 и п.8 в README, логотип карточки в README (включён по умолчанию, где выключается), п.9 — ссылки `lumen-phase5-tv-fix.md:340` и `src/30_css.js:1733-1737` в разборе композиции, этот раздел и раздел D.

### Контрольное ревью 84c7b27..de0e2c8 и четвёртый фикс-раунд (2026-09-24)

Независимое ревью всех трёх фикс-раундов (диапазон `84c7b27..de0e2c8`): **5 находок, блокеров нет** — одна Major, две Minor, одна сомнительная, одна по документам. Все закрыты четвёртым фикс-раундом, каждая — своим коммитом с тестом, падавшим до правки:

- `5e70941` п.1 (Major) — рулетка оставалась пустой навсегда, если уйти через меню, пока каталог шёл по сети: `pause()` -> `bump()` отсекал ответ `LC.manifest.load` тем же общим `gen` (регрессия `20b5f25`, на `84c7b27` работало). Ответ каталога теперь гасит только `destroy()`; пришедший на паузе строит экран без показа выборки, её поднимает `start()`; `start()` без каталога и без запроса в полёте спрашивает его заново. Стенд тестов рулетки научился отвечать асинхронно (каталог и пул), как настоящая Lampa.
- `89fa857` п.2 (Minor) — индикатор загрузки висел после ухода во время первого вращения: снимал его только колбэк `loadPool`, отсечённый `bump()`. `start()` при готовом каталоге снимает индикатор.
- `ec2fcbe` п.3 (Minor) — скелетоны героя не пульсировали в «Полном»: правило проявления `.lumen-hero__text.is-in > …` специфичнее `.lumen-skeleton`, а `is-in` висит до смены карточки. Скелетоны исключены из проявления (`:not(.lumen-skeleton)`).
- `65dc9b8` п.4 (сомнительная — подтверждена по коду Lampa) — скелетон героя оставался после возврата без карточки в фокусе. Сценарий: фокус с карточки на плитку «Ещё» ряда (`.card-more`; Lampa пишет её в `last` ряда), OK, пока детали в пути; на возврате `items_line.toggle` фокусирует ту же плитку, и `resume()` выходил без `show()`. Теперь при `stale` без карточки в фокусе показывается заново последний показанный фильм (`state.shownCard`).
- коммит документов, п.5 — README (раздел возможностей) и `docs/tv-checklist.md`: режим постеров «Английские», а не «Оригинал», цена — как в строках настроек; этот раздел и правда о публикации выше.

### Контрольное ревью de0e2c8..0c79048 и пятый фикс-раунд (2026-09-24, не запушен)

Ревью четвёртого фикс-раунда: **6 находок, блокеров нет** — три о том, что стенд тестов рулетки не видел проверяемого (сторожа держались, но тестом не ловились), две к проверке в рулетке, одна косметика героя. Каждая закрыта своим коммитом; тесты сторожей проверены временной поломкой кода (падают — код возвращён), тесты правок падали до правки. Итог: **2114 тестов, 0 падений**, ES5 ok, `dist` пересобран (`build --check` чистый).

- `a67811c` п.1 — сторож двойного запроса каталога (`if (manifestWait) return;`) под тестом: заглушка каталога считает вызовы; «каталог после возврата» — ровно один запрос, `start/pause/start/pause/start` до ответа — один запрос каталога и один сбор выборки. Без сторожа — 3 и 4 запроса.
- `e99c039` п.2 — у заглушки `El` не было `text()`, `paintPreview` на стенде всегда падал в `try/catch`, `is-stack` не ставился никогда. `text()` как у jQuery; тесты показа выборки проверяют `is-stack` и счётчик; новый тест на ветку `start()` «выборка уже на экране — показ не поднимается заново». Скрытых дефектов `text()` не вскрыл.
- `fb0cd4a` п.3 — заглушка пула отбрасывает ответ по `alive()` и после `clear()`, как настоящий `fetchAll` (`src/43_sources.js:461-500`); контрактный тест самой заглушки.
- `e61aa48` п.4 (подтверждено) — «Крутить», пока показ выборки ждёт сеть, заводил второй сбор пула: второй набор `LC.sources.fetch` (дедупликация `fetchAll` спасала лишь подборки, ещё не ответившие). Теперь `loadPool` того же набора в том же поколении встаёт в очередь сбора в полёте (`poolWait`).
- `66b9329` п.5 (подтверждено по коду Lampa) — `backward()` уничтожает рулетку через 200 мс без `pause()` (`app.min.js:45933-45950`); ответ каталога в это окно забирал коллекцию Navigator у экрана, на который вернулись. `build()` ставит коллекцию только активной рулетке (`Lampa.Activity.active()`, как в `start()`).
- `49899e9` п.6 — `park()` героя ставил `stale` и за один таймер фокуса другой карточки: «A загружен -> B быстрее DELAY -> «Ещё» -> OK -> Назад» заново показывал A со вспышкой скелетона. `stale` теперь — только оборванная загрузка показанного фильма; сценарий прошлого раунда (детали A оборваны) по-прежнему показывается заново.
- коммит документов — этот раздел и «Состояние» выше.

---

## A. Дефекты — чинить до релиза, одной волной, одно ревью

| # | дефект | факт | объём |
|---|---|---|---|
| **A1** | При управлении **мышью** герой на главной не переключается; то же — плитки хаба, сетки, серий, чипы | Lampa при мыши шлёт `hover:hover`, не `hover:focus` (`app.min.js:46360-46364`); мы подписаны только на второе. Воспроизведено на стенде | в работе |
| **A2** | Метка в подписи (`lumen_badges=caption`, стиль Apple TV) съедает год и рейтинг: «Новинка · 2026 · ★ 7.3» просит 125 px при 109 | замер координатора на стенде 960×540@2 | малый: при наличии метки в подписи не показывать рейтинг (метка · год), либо метка вместо года — решить одним замером ширины на всех четырёх масштабах |
| **A3** | На постере три сообщения об одном прогрессе: плашка «49 %», полоса внизу, подпись «Просмот…» посередине | скрин пользователя 2026-09-22 (базовый стиль) | малый: оставить полосу и один текст; на каком экране что — по скрину |
| **A4** | «Досмотреть» держит фильм на 85 % | скрин; порог выпадения сейчас 95 % (Task 58, совпадает с меткой Lampa «просмотрено») | **решение пользователя**: опускать порог (например до 85–90 %) или оставить. Без решения — не трогать |
| **A5** | Плоский вид почти не меняет сетку и хаб — описание обещает больше, чем есть | ревью Task 73, подтверждено исполнителем | малый: либо снять сетку/хаб из описания настройки (честно), либо дать им заметное отличие. Рекомендация — снять из описания: там и так нет коробок |
| **A6** | Блоки «Метаданные» (Темп/Страх/Экшн) и «Настроения» (проценты) на карточке — не наши и не в `app.min.js` (grep пуст), пользователь говорит, что плагины отключены | скрин 2026-09-22 | **выяснить, не чинить**: одна проверка — приходят ли они от аккаунта CUB (в консоли Lampa `Account - 3`). Если это штатная функция аккаунта — сказать, где выключается; если плагин — назвать |
| **A7** | **Найден по ходу волны, в исходной таблице его не было.** «Размер интерфейса» Lampa не учитывался в порогах раскладки главной | ревью волны A (важное 1) + живая проверка на стенде | закрыт, см. ниже |

Порядок: A1 (идёт) → A2 → A3 → A5 → A6 → A4 после решения. Затем **одно ревью** на волну A, фикс-раунд, push, purge.

Оценка честная: A2+A3+A5 — около 2–3 часов работы с ревью; A1 — до часа после ответа исполнителя; A4/A6 — по ответу пользователя.

### Статусы — волна A закрыта 2026-09-22

- **A1 — закрыт** (`5436c35`, поправка `3e1d096`). Заведён общий `src/11_focus.js` (`LC.focus.EVENTS` — `hover:focus` и `hover:hover`, три способа подписки), на него переведены все 13 мест: герой, хаб и сетка, рулетка, отзывы, ряд серий; сторож в `test/focus.test.mjs` запрещает имена этих событий где-либо, кроме модуля. Третье имя Lampa, `hover:touch`, сознательно не подключено — тач никто не проверял, и это решение тоже пиньется тестом.
- **A2 — закрыт** (`6c4becd`, доделка `1e1ecfb`). При метке в подписи рейтинг туда больше не дописывается — остаётся «метка · год» (77.7…87.3 px против доступных 98…110). Выброшен рейтинг, а не год: плашку `.card__vote` на главной мы и так прячем. Доделкой закрыт побочный дефект: правило было общим для сетки и хаба, где плашка тоже спрятана, и карточка с меткой оставалась без оценки вовсе — там рейтинг вернулся флагом `opts.wide`.
- **A3 — закрыт** (`b58b2be`, поправка комментария `dfedaab`). Третье сообщение рисовали не мы: это штатная панель `.card-watched` Lampa (компонент Watched, `app.min.js:21870`, показывается через 500 мс после фокуса при настройке `card_episodes`). Скрыта панель, а не наши узлы — её текст рендерится кеглем 9.12 CSS px при `TV_MIN = 11.52` и обрезан всегда. Навигация цела: внутри `.card-watched` нет ни одного `.selector` (первое объяснение через `offsetParent` оказалось неверным и поправлено).
- **A4 — закрыт** (`f780399`). Решение принято: **порог опущен до 90 %**, не до 85. Константа одна — `CONTINUE_DONE` (`src/45_personal.js`), одно сравнение, сторож в тестах. Отметку Lampa «просмотрено» правка не трогает: её ставит сама Lampa числом 95, и три наших места с `WATCHED = 95` остались на 95.
- **A5 — закрыт** (`79c1a75`). Выбран честный вариант — **снять из описания**: в сетке и хабе плоский вид снимает ровно три подложки, и все три лежат под картинкой, то есть видны только у плиток без пришедшего кадра (замер: 25 из 34 на экране «Подборки»). Описание настройки и README переписаны под этот факт на трёх языках, набор правил закреплён сторожем в `test/css.test.mjs`.
- **A6 — закрыт** (`dc66c77`, поправка комментария `4443286`). Выяснено построчно: «Метаданные» и «Настроения» — **штатные блоки самой Lampa**, данные приносит `Api.sources.cub.metadataGet` (то есть аккаунт CUB) и только для фильма; пункта настроек у Lampa нет, есть только флаг сборки. Раз выключить их нечем, сделана **своя настройка `lumen_hide_meta` «Скрывать блоки анализа Lampa»** — по умолчанию выключена (чужие данные молча не прячем), в `PRESET_APPLETV` включена. Ряды не прячутся, а не создаются: на событии `full`/`start` снимается `data.metadata` и возвращается на `complite`. Два других пути отвергнуты живой проверкой — `display:none` и снятие узла ломали навигацию вниз.
- **A7 — закрыт** (`9c87a01`, следом `8074aba`). Оба порога раскладки (`textRatio` и `rowNarrowRatio`, `src/30_css.js`) считали ширину экрана как 84.17 базовых em безусловно, то есть верно только при «обычном» размере интерфейса. Множителей у Lampa **два**: базовый кегль `body = innerWidth / 84.17 × k` (`vendor/lampa/app.min.js:31629-31640`, `small 0.9 / normal 1 / bigger 1.05`) и отдельное правило `@media screen and (min-width:767px){body.size--bigger .card{font-size:1.14em}}` (`vendor/lampa/css/app.css:3525-3528`) — второе действует только на «крупнее» и только внутри карточки, итого ×1.197 против обычного. Порог 767 — ширина окна в CSS px, а телевизор отдаёт WebView 960, значит правило там работает; это и были те ~13 px, которых ревью не могло объяснить одним `k`. Обе таблицы живут в одном месте (`LAMPA_SIZES`, `LAMPA_CARD_SIZES`, `src/10_util.js`), модель инварианта гоняется по 3 размерам интерфейса × 4 масштабам × 3 размерам кадра. Вскрытые этим три клетки за пределом (537.5 / 555.9 / 574.1 px при 532) закрыл `8074aba` — потолок масштаба карточки по тому же бюджету высоты; из 72 клеток изменились ровно 6.

---

## B. Проверка на телевизоре — только пользователь

Без этого раздел «готово» подтвердить нечем. Нужно **после** волны A, одной серией, по обновлённому чек-листу (`docs/tv-checklist.md`, раздел 14):

| снимок | что проверяем | какой пункт закрывает |
|---|---|---|
| B1 главная в покое, HUD | fps, `long` за окно, `raf` по корзинам | критерий 2 |
| B2 главная при листании, HUD | просадка, длинные задачи | критерий 2, судьба Task 65 |
| B3 карточка «Дораэмон», HUD через 3–5 с после открытия | `eps 25`, fps без провала | Task 67 на устройстве |
| B4 главная, фокус во втором ряду, без HUD | кадр героя виден и сдвинут | Task 70 |
| B5 главная, листание по ряду, без HUD | название/логотип меняется без подмены на ходу | Task 71 + A1 |
| B6 рулетка после «Крутить» | барабан крутится 8 раз | Task 72 |
| B7 стартовая в «Как Apple TV» **после повторного нажатия кнопки** | плоский вид, метки в подписи не режут год | Task 73, A2 |
| B8 путь TorrServer, фокус на второй раздаче | рамка фокуса целая сверху | фикс-раунд волны 6 |

Если B3 показывает провал — виртуализация ряда серий не помогла на устройстве, и причина другая (Lampa строит свои блоки); это единственный сценарий, где потребуется ещё одна диагностическая задача.

---

## C. Закрытие

| # | что | кто | статус |
|---|---|---|---|
| C1 | Отметить Task 67/70/71/72/73/68 «ВЫПОЛНЕНО» в плане фазы 6 с SHA и отступлениями (сейчас не отмечены — волна шла без остановок) | координатор | **выполнено 2026-09-22** — отметки стоят под заголовками задач в `docs/plans/2026-09-22-lumen-phase6-tv-feedback.md`, чекбоксы закрыты, в разделе «Порядок» записано, что волна пройдена и снимков HUD ещё не было |
| C2 | README: раздел «Управление мышью» (после A1), описание «Плоского вида» по факту (после A5) | исполнитель волны A | **выполнено** — раздел про мышь `5eead3f` (ревью волны A нашло, что пункт был пропущен), описание плоского вида `79c1a75` |
| C3 | Память проекта: итог фаз 5–6, правило «стенд проверять в обоих режимах управления — пультом и мышью» (урок A1), правило «часовая метка в загрузчике — не убирать» | координатор | **за координатором** — в репозитории следов не оставляет, отсюда не проверяется |
| C4 | Финальный `git show --stat` по каждому коммиту волны — сделано 2026-09-22 для волны 6 (все 11 коммитов только в ожидаемых файлах); повторить для волны A | координатор | **выполнено 2026-09-22**, см. ниже |
| C5 | Версия: `dist` баннер сейчас `v0.2.0`; перед релизом поднять до `v1.0.0` в одном месте, откуда он берётся (`scripts/build.mjs` или `00_head.js` — найти, не плодить) | исполнитель | **НЕ ДЕЛАТЬ СЕЙЧАС**, см. ниже |

**C4 — фактический состав волны A.** `git diff --stat 119711e..HEAD` (то есть от последнего коммита фазы 6 до `8074aba`) даёт **29 файлов**, посторонних путей среди них нет:

- `README.md` и `dist/lumen_card.js` — ожидаемые;
- **один** план — `docs/plans/2026-09-22-lumen-final.md` (план фазы 6 волна A не трогала; его правит только отметка C1, уже после этого замера);
- **13 файлов `src/`:** `10_util.js`, `11_focus.js` (новый), `30_css.js`, `45_personal.js`, `46_hub.js`, `48_hero.js`, `56_roulette.js`, `60_reviews.js`, `62_badges.js`, `80_settings.js`, `81_prefs.js`, `85_header.js`, `90_runtime.js`;
- **13 файлов `test/`:** `_load.mjs`, `badges`, `css`, `focus` (новый), `header`, `hero`, `hub`, `menus_runtime`, `personal`, `prefs`, `reviews`, `roulette`, `settings`.

Итого 1 + 1 + 1 + 13 + 13 = 29. (Предварительная оценка «два плана и 12 файлов `src/`» не сошлась: план один, файлов `src/` тринадцать.)

**C5 — версию сейчас не поднимать. Решение координатора 2026-09-22.** `LC.VERSION` живёт в `src/00_head.js`; поднять её до `1.0.0` нужно **последним шагом, после раздела B**. Пока проверка на телевизоре не прошла, критерий «готово» не выполнен, и номер версии в баннере врал бы — `v1.0.0` на сборке, которую никто не видел на устройстве, дороже обходится, чем неудобство лишнего шага. Поэтому C5 остаётся открытым пунктом до закрытия B, а не галочкой.

---

## D. За релизом — не делать до «готово»

- **Task 65** виртуализация рядов главной — только если B2 покажет длинные задачи на шаге фокуса.
- Ряд широких плиток 16:9 вместо постеров — упирается в высоту и Task 65.
- Чипы настроения отдельным рядом — закрыто замером (не хватает 32 px), оба обхода не работают.
- ~~Разбор композиции дизайнером по скринам — после релиза~~ — **сделан раньше релиза** (`d550f93`, 2026-09-22) и большей частью выполнен волнами выше; что сделано и что нет — таблица «Что сделано» в конце `docs/design/2026-09-22-composition-review.md`.
- «Как Apple TV» для меню и рулетки — там уже нейтральный фокус, отличий нет; не делать.
- Пороги автодетекта — не трогать: пользователь выбрал режим сам.
- **ЗАКРЫТ `dc13bbd` (2026-09-23).** ~~Долг, найденный при разборе A7: пол базового кегля Lampa 10.6 px модель раскладки не знает.~~ Теперь экран в em считает `LC.util.screenBaseEm()` от того же `baseEm()`, где пол учтён (тело коммита). Исходная запись — ниже, без правок: Lampa ставит `fontSize: Math.max(window.innerWidth / 84.17 * fs, 10.6)` (`vendor/lampa/app.min.js:31629-31640`) — **нижняя граница 10.6 px**, а `LC.util.lampaSizeK` (`src/10_util.js:136-157`), `screenEm()` (`src/30_css.js:692-700`) и `LAMPA_SIZES` в `test/css.test.mjs:2305` считают экран как `84.17 / k` без всякого пола. Пол включается при ширине окна меньше `10.6 × 84.17 / 0.9 = 991.3` CSS px, то есть **на всех окнах уже 991 px**, включая WebView телевизора (960). Следствие на «мельче» при 960 px: фактический кегль 10.6 вместо 10.264, экран содержит `960 / 10.6 = 90.57` базовых em, а модель берёт 93.52 — то есть **ширина экрана в em завышена примерно на 3.3 %**, и оба порога (узкая колонка `rowNarrowRatio`, потолок масштаба `rowScaleCap`) включаются **позже**, чем должны. Сейчас это ничего не ломает: все 72 клетки прогона раскладки в `test/css.test.mjs` укладываются в предел 532. **Но комментарий в `src/30_css.js:682-685` («84.17 при „обычном“, 93.52 при „мельче“ и 80.16 при „крупнее“») с приходом пола перестал быть правдой для „мельче“ на узких окнах**; та же неполная формулировка — в `src/10_util.js:138-144`. Код и комментарии этой правкой не трогались сознательно: долг зафиксирован здесь. Чего не проверял: сколько именно запаса остаётся у самой тяжёлой клетки «мельче» — число в модели не запинено, и своего замера я не делал.

- **ЗАКРЫТ `49b74ca` (2026-09-22).** ~~Долг, найденный финальным ревью: ширина плитки хаба и карточки сетки считается без учёта размеров.~~ Ширина теперь из той же цепочки, что раскладка (замер по 24 клеткам — в теле коммита). Исходная запись: `TILE_EM`/`GCARD_EM` (`src/46_hub.js:86-93`) прибиты к `84.17` — без `lampaSizeK()` и без `uiScale()`, хотя ширина задана процентами контейнера, а `LC.util.emPx` домножает на масштаб плагина. Замеры исполнителя (2026-09-22): при «крупнее» и штатном масштабе расчёт завышен на **7.8 %** (5 % даёт `lampaSizeK`, 1.9 % — устаревший в формуле отступ: комментарий считает `2.81em` по краям, в CSS `EDGE = 3.51em`, `src/30_css.js:2102`); от `lumen_scale` расхождение доходит до **+25.6…+32.8 %** на «огромном» и до **−4…−9 %** на «мельче». **Ошибка двусторонняя, и в одну сторону она портит картинку:** при 1920 CSS px, DPR 1, Lampa «мельче» и `lumen_scale = 0.9` плитка хаба шириной 435 px получает с TMDB **w300 вместо w780** — видимое мыло. На целевых экранах (960 CSS px при DPR 2 и 1920 при DPR 1) ступень не расходится ни в одной из 12 проверенных клеток, поэтому релиз это не держит. Отдельной задачей.
- **Перемерено 2026-09-23 (после `363bc8d`), модель теста Task 51, 960×540, худший случай — полные анимации:** «крупнее» + средний кадр + «огромный» — низ подписи 527.0 px при пределе 532 (масштаб карточки не срезан, 1.20); «обычный» + крупный + «огромный» — 527.5 (1.20). Самая тесная клетка теперь другая — «крупнее» + крупный + «огромный»: 530.7, масштаб срезан потолком до 1.00. Запись ниже — история.
- **Две клетки бюджета высоты стоят на самом краю — знать при любой правке геометрии ряда.** «Крупнее» + средний кадр + «огромный» масштаб: запас **0.031em ≈ 0.37 CSS px** (блок 23.889em при бюджете 23.920em). «Обычный» + крупный кадр + «огромный»: **0.046em**. Следующая правка ширины карточки, зазора или кегля подписи уронит их, и потолок `rowScaleCap` начнёт срезать масштаб уже и на среднем кадре — это будет не регрессия, а работа защиты, но заметят её как «масштаб перестал меняться». Обе клетки прогоняются в `test/css.test.mjs`, молча не пройдут. **Оговорка 2026-09-23:** числа запаса сняты до `6d3c2c7` (шапка ряда мерится по заголовку, −6.5 px в цепочке высот, потолок масштаба поднялся с .96 до 1.00) и `dc13bbd` (пол кегля 10.6 в ширине экрана); после них эти две клетки не перемерялись — актуален ли запас, не установлено.
- **Долги, записанные 2026-09-23 (волны постеров и долгов).**
  - **Оверскан фона карточки не знает `screenPx()`.** В режиме движения `full` у активного кадра фона карточки идёт наезд Ken Burns `scale(1) → scale(1.08)` (`src/30_css.js`, правило `lumen-kb`), и в конце наезда слой шире экрана: замер на стенде 1920×1080, DPR 1, фильм 1891 — прямоугольник кадра 2073.6 CSS px при экране 1920, `matrix(1.08, 0, 0, 1.08, 0, 0)`, растяжение `w1280` ×1.62 вместо ×1.50. Размер кадра выбирается `LC.util.frameSize(LC.util.screenPx())` (`src/50_backdrops.js:48, 361`) по ширине экрана без этого множителя. В `lite` (режим пользователя) наезда нет — тот же замер даёт слой 1920 и ×1.50. Найдено при работе над постерами; не правилось.
  - **ЗАКРЫТ `b99531c`.** ~~**Герой главной пересобирается на каждом возврате из карточки** (`src/48_hero.js`: `detach()` на `start` чужой активности, `mount()` заново на возврате). Вторая половина долга фазы 2, в `4cb66b5` сознательно не вошла (записано в теле коммита).~~ Уход вглубь паркует героя, возврат будит его с теми же узлами и кадром; слайдшоу «Несколько кадров» продолжает счёт.
  - **ЗАКРЫТ `44086b1` (п.5) и `78d0bdb` + `34c8d94` (п.4).** ~~**Рефакторинг двух долгов фазы 1** (`docs/plans/2026-09-15-lumen-card.md:1123`, пп. 4 и 5): «наша ли карточка на экране» отвечается несколькими разными способами (ревью фазы 1 насчитало пять; пересчёт не делался), и `LC.pref` соседствует с `Lampa.Storage.field` для наших же ключей — 4 места, по grep 2026-09-23: `src/55_trailer.js:119` (`lumen_trailer`), `src/81_prefs.js:655` (`lumen_motion`), `src/90_runtime.js:1105` и `:1298` (`lumen_menus`).~~ Пересчёт: способов было шесть, мест с `Storage.field` для своих ключей — пять.
  - **ЗАКРЫТ `7a0e93e`.** ~~**Ширину полосы прогресса «Продолжить» не проверяет ни один тест.**~~ `refreshProgress` ставит ширину через `row.find('.lumen-progress__bar > div')` (`src/85_header.js:454`), а фейковый DOM (`test/_fakedom.mjs`, `selectorClasses`) режет селектор по точкам и ищет класс `lumen-progress__bar > div` — такого нет, `find` отдаёт пустой набор, и `css('width', …)` уходит в пустоту. Узел заливки в тесте (`pFill`, `test/header.test.mjs:102`) строится, но его ширину никто не читает. Родственный долг — п. 6 фазы 1 (`find()`/`children()` фейка отдают первый элемент, а не набор); здесь другая дыра того же фейка — комбинаторов селектора он не разбирает.

### Что остаётся открытым в планах после волны 2026-09-23 (и почему)

Всё, что можно было закрыть без телевизора и без решения пользователя, закрыто этой волной или закрыто раньше (отметки — в самих планах). Остальное:

**Нужен телевизор (раздел B и замеры HUD):**
- Раздел B целиком (B1–B8) и критерии 1–2 «готово»; C5 (версия `1.0.0`) — последним шагом после B.
- Task 65, виртуализация рядов главной — только если B2 покажет длинные задачи.
- Фаза 4: горизонтальные маски рядов (`.scroll--horizontal.scroll--mask`, `app.css:2798`) и `.card{will-change:transform}` Lampa — цена в слоях видна только в HUD устройства, снятие рискует вернуть шлейф Task 46.
- Фаза 2, долги 1 (цена первого экрана главной), 6 (переход «вверх из первого ряда на чипы» физическим пультом), 8 (реальные Tizen / webOS / Android TV).
- Раздел D, оверскан фона карточки при Ken Burns (`w1280` растянут ×1.62 в конце наезда): выбор между `w1280` и `original` — размен памяти и декодирования на ТВ против резкости в режиме «полный», который у пользователя не включён (`lite`); решать по замеру на устройстве.

**Решение пользователя:**
- Публикация в `main` и push.
- Ряд широких плиток 16:9 вместо постеров (раздел D).
- Фаза 4: `lumen_scale` не действует на текст и логотип героя — так устроено намеренно (`src/30_css.js`, комментарий у `LC.uiScale`, с `b38425a`: у кадра высота считается от экрана, текст героя — свой `TEXT_ZOOM`); распространить масштаб на героя — вопрос вида.
- ~~Фильмы «Звёздных войн»: коллекция TMDB 10 — сага из 9 фильмов, «Изгой-один» и «Хан Соло» в неё не входят~~ — **решено координатором и сделано `b1c5abf`** (см. «Открыто после независимого ревью»).

**Сознательный отказ (довод записан):**
- «Как Apple TV» для меню и рулетки; пороги автодетекта (раздел D).
- Фаза 2, долг 7 — встроенный каталог дублирует хостинговый (выигрыш 6 % размера против пустой главной без сети); долг 3 — мини-герой в сетке (README: содержимое сетки не прокручивается с шапкой, герой занимал бы место постоянно).
- Фаза 3: `showJump()` — не сделан, довод в теле `63a8a78`.
- Фаза 4: `test/menus_runtime.test.mjs` сверяет журналы активации полным `deepEqual` — оставлено: точный журнал ловит лишний или повторный вызов при активации, а сверка «только порядка нужных» его бы пропустила; цена — ручная правка ассерта при новой подсистеме.
- Правило кромки за порогом, где кадра героя нет (21:9 и шире), доведено (`363bc8d`), но на высоких окнах (4:3 и уже) зазор под фокусным рядом растёт до пустой полосы — ничего не выглядывает, это и есть цена правила «без выступа».

### Открыто после независимого ревью (2026-09-23, после Ф3)

**Нужен телевизор:**
- Все три фикс-раунда проверены тестами и стендом (960×540, DPR 2), не устройством; четвёртый и пятый — только тестами. Раздел B остаётся как был.
- Д1 (`8ef31cd`): атмосфера на телевизоре пользователя не рисуется вовсе — у него `lite`, а слой частиц есть только в `full` с тяжёлыми эффектами (`allowedNow`, `src/52_fx.js`). Правка видна только в полном режиме; на стенде проверена там.

**Решение пользователя:**
- **Полная сетка Lampa у сериальных подборок и реалити с жанром Talk.** Ряды, сетка подборки и рулетка отсекают ток-шоу в ответе (`talkOnly`) и реалити 10767+10764 оставляют. Но 14 подборок только из discover/tv открываются в штатную `category_full` Lampa (`openTarget`, `src/46_hub.js`), а её ответ фильтровать нечем — там запрет `without_genres=10767` остался в запросе. Пример (живой запрос 2026-09-23): The Grand Tour (10764,35,10767) стоит на 3-й странице подборки «Amazon Prime» (`networks: 1024`) без запрета; наши пути (ряд, рулетка) его больше не выбрасывают, а в полной сетке Lampa, куда эта подборка открывается, его нет. Выбор сделан по данным: на первых страницах этих 14 подборок ток-шоу 15, реалити 10767+10764 — два. Альтернатива — открывать такие подборки своей сеткой `lumen_grid`, где фильтр есть; это смена поведения «Ещё», не правка данных.
- **Кэш ответов «Без надписей» (`movie|tv/{id}/images`) копится в IndexedDB без вытеснения.** Замер 2026-09-23 (прокси Lampa, `include_image_language=null`, JSON ответа): набор главной по умолчанию — 151 карточка, 1.2 МБ, в среднем 7.9 КБ на фильм (медиана 6.6, 90-й процентиль 15.7, максимум 42.0). Прежний замер по десяти популярным фильмам — 17.5 КБ на фильм (`src/43_sources.js`, у `cleanPosters`); у сериалов и аниме главной постеров меньше. Ключ записи — адрес запроса и язык интерфейса (`cacheName`, `vendor/lampa/app.min.js:33548-33556`), поэтому повторный показ того же фильма запись перезаписывает, а растёт кэш только числом РАЗНЫХ фильмов: первое открытие главной — 1.2 МБ, каждая пролистанная страница сетки — ещё 20 фильмов, около 160 КБ при среднем. Сколько разных фильмов человек видит за месяц, не установлено; при 1000 — это 7.7 МБ по среднему главной и 17.1 МБ по среднему популярных. Вытеснения нет ни у нас, ни у самой Lampa (её собственные ответы копятся тем же способом); чистит кэш только пункт Lampa «Остальное → Очистить кэш» (`Cache.clearAll`, `app.min.js:54987`). Режим выключен по умолчанию («Как в Lampa» — ни одной записи). **Предложение, не сделано:** если цена окажется больше единиц мегабайт на деле — держать в `localStorage` список ключей своих записей и удалять самые старые сверх порога (например, 1000 фильмов) через `Cache.deleteData('other', key)` (`app.min.js:16677`). Принимать ли цену как есть — решение пользователя.

**Сознательный отказ (довод записан):**
- **Двухуровневое название** (`a2513e6`): «Puella Magi Madoka Magica the Movie -Walpurgisnacht: Rising-» режется по двоеточию внутри японского подзаголовка в дефисах-скобках — текстом его не отличить от честного разделителя; семь честных разрезов вида «Матрица: Перезагрузка» ушли в фолбэк длинного названия — это цена снятия «Mission / Impossible III». Выборка и числа — в теле коммита.
- **«Кино о любви»**: «Злая» остаётся (у неё на TMDB стоит драма); запрет фэнтези выбросил бы «Сумерки», «Эдварда руки-ножницы», «Знакомьтесь, Джо Блэк», «Бойфренда из будущего» (`b1c5abf`).
- **«Звёздные войны» запросом, а не списком id**: в подборку вошли ещё «Войны клонов» (2008) и «Мандалорец и Грогу» (2026) — это франшиза; цена та же, что у сериалов по студии: фантастика Lucasfilm вне франшизы с 200 голосами попала бы в подборку (сейчас такой нет; единственное чужое без порога — «Captain EO», 55 голосов).
- **Трейлер из меню карточки** (`src/63_cardmenu.js`, `Lampa.Api.sources.tmdb.videos`): ответ, доехавший после ухода с карточки, всё равно запустит ролик. Это не `tmdb.get` (за пределами Д2), ролик человек заказал сам, и штатная кнопка Lampa ведёт себя так же; не правилось.

---

## Почему было долго — и что меняется

Три причины, все настоящие:

1. **Каждое ревью находило 5–10 реальных дефектов.** Без него на телевизор приезжали бы пустые чекбоксы, обрезанные метки, невидимый фокус, подпись на кадре. Ревью остаётся, но одно на волну, не на задачу.
2. **Ложные посылки координатора** стоили примерно по раунду каждая: «TMDB со стенда недоступен», «в lite теряется заливка», выдуманная цитата Apple, «lite выставил автодетект». Правило записано в память: возражение исполнителя проверять замером, не настаивать.
3. **Стенд проверялся только пультом.** Дефект A1 жил с первой версии и не ловился ни одним тестом и ни одной живой проверкой — потому что стенд никогда не гоняли мышью. С волны A каждая живая проверка идёт в обоих режимах управления.

И одно организационное: **новых задач в этот план не добавляется.** Всё, что придёт после, — либо дефект в A, либо раздел D.

## Что НЕ делать (переносится)
- `HERO_VH.large` не трогать; `main` не пушить без решения пользователя; настройки Lampa из кода не менять; ключ Kinopoisk не вводить; `original` для кадра не возвращать; инвариант «низ подписи ≤ 532» не ослаблять; каждое утверждение в комментарии проверяемо.
