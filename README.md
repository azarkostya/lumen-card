# Lumen Card

Плагин для [Lampa](https://lampa.mx) (Smart TV / Android TV), который перерисовывает карточку
фильма и сериала, достраивает главную и добавляет свои экраны подборок — бесплатная альтернатива
Cardify.

Тёплая «дюнная» палитра, полноэкранный кадр с градиентными вуалями, крупный акцентный фокус кнопок,
читаемый с трёх метров, строка «Продолжить» с таймкодом и ряд серий сезона.

## Что умеет

**Карточка**

- Полноэкранный кадр со слайдшоу и наездом Ken Burns, беззвучный трейлер в фоне через 3 с после
  открытия, мета-строка вместо штатной, таблица «ПОДРОБНО», ряд серий сезона.
- Строка «Продолжить» с таймкодом и процентом, подпись «Продолжить S2 E3» на кнопке «Смотреть».
- Отзывы зрителей Кинопоиска с тоном и «полезно»; спойлерные куски замазаны и открываются кнопкой.
- Ряд «Смотреть по порядку» для фильма из франшизы: номер части, отметка просмотренного, «Вы здесь»
  и «Дальше»; порядок — по годам или по рейтингу.
- Логотип названия вместо набранного текста — тот же, что в кадре главной (настройка «Логотип
  названия в карточке», по умолчанию вкл); нет логотипа — двухуровневое название текстом.
- Первый экран карточки — шапка целиком; ниже обычная прокрутка: описание, отзывы и «Смотреть по
  порядку» одним рядом. Блок, куда пульт перевёл фокус, встаёт к верху экрана, если уходил за
  кромку; мышь страницу под курсором не двигает, колесо доводит до следующего блока и ставит на
  него фокус — следующее нажатие пульта продолжает с показанного места.
- Карточка открывается без эффектов: размытый фон Lampa под ней не проступает, кадр встаёт сразу.
- Акцент, посчитанный по постеру открытого фильма (настройка): цвет кнопок, колец фокуса и
  подсветок берётся из самой картинки.
- Тематическая атмосфера поверх кадра: снег, летучие мыши, звёзды, дождь, песок, пузыри, лепестки,
  искры, помехи — по ключевым словам фильма. У праздников — сцены в несколько планов: Хэллоуин —
  тлеющие угли с ореолом, летучие мыши-силуэты и дымка по низу; Рождество и Новый год — снег в трёх
  планах, боке огней и гирлянда с бегущим мерцанием; День святого Валентина — сердца и розовое боке.
  Размер частиц — доля экрана, а не пиксели: на мониторе 2K они той же величины, что на телевизоре;
  у текста карточки и героя частицы гаснут до 15 %.

**Путь до плеера**

- Экраны от кнопки «Смотреть» до плеера — выбор источника, список раздач TorrServer, окна
  подключения и ошибок, списки файлов, предзагрузка — в стиле карточки, без единой правки логики
  и разметки Lampa.

**Главная**

- Герой — кадр фильма без надписей на весь экран (неподвижный: ряды наезжают на него снизу),
  логотип, мета с двумя жанрами и две строки описания фильма под фокусом; через 8 с покоя фокуса
  кадр сменяется беззвучным трейлером. Размер героя выбирается в настройках, вплоть до «выключен».
- При листании герой не догоняет каждое нажатие: одиночное нажатие меняет героя через 0,35 с, а
  серия нажатий (следующее — раньше чем через 0,7 с после прошлого) — один раз, через 0,7 с после
  последнего, на карточке, где листание кончилось. Детали и логотипы соседей при этом
  подгружаются на каждом шаге, и герой встаёт сразу целиком. В «Полных» анимациях увеличение
  карточки в серии — без плавного перехода.
- Кадр героя не повторяет постер карточки под ним: кандидаты сравниваются с постером по
  миниатюрам (w92, в простое браузера), первым встаёт непохожий, похожие не идут и в смену кадров;
  пока кадр грузится, под текстом — нейтральный фон, а не постер. Тёмный логотип (чёрный на тёмном
  кадре) заменяется светлым вариантом на том же языке, а если такого нет — рисуется белым
  силуэтом; так же и в карточке фильма. Работает там, где прокси картинок отдаёт CORS-заголовок
  (прокси Lampa и image.tmdb.org — да); иначе всё как раньше.
- Профили настроения — строка быстрых подборок: «Вечер пятницы», «Семейный просмотр», «Страшное на
  ночь», «Есть 90 минут». Стоит над рядами, когда кадр над рядами выключен; при кадре её нет —
  те же подборки есть в хабе и меню.
- Персональные ряды: «Досмотреть», «Потому что вы смотрели «…»», «Новые серии ваших сериалов»,
  «Скоро на экранах» — из истории и закладок самой Lampa.
- Ряды подборок: франшизы, студии, сервисы, темы, страны, эпохи, режиссёры, топы и подборки
  Кинопоиска; состав выбирается галочками. В декабре добавляется ряд «Адвент-календарь».
- Сезонные подборки в свой месяц встают в начало хаба с меткой «Сезон»: рождественские и
  новогодние (декабрь–январь), «Хэллоуин» (сентябрь–ноябрь), «Кино о любви» (февраль), «Кино о
  войне» (май).
- Источник постеров — на выбор, у каждого режима своя цена: «Как в Lampa» (по умолчанию) — ни
  одного лишнего запроса; «Английские» — английская обложка (не на языке оригинала), запрос на
  каждую половину подборки, то есть один-два на ряд; «Без надписей» — постер без текста, по
  запросу на каждую карточку — двадцать на ряд из одного списка, до сорока у рядов с фильмами и
  сериалами, дальше из кэша Lampa на 30 дней — пока в самой Lampa включено «Кэширование
  запросов». Подробности — в таблице настроек.
- Метки на постерах: «Скоро · 17 дек», «Новинка», «Продолжить · 43 %», «Новая серия».

**Подборки**

- Хаб «Подборки» в левом меню: группы чипами, плитки с коллажами из постеров, поиск по названию
  через штатную клавиатуру Lampa (с микрофоном там, где Lampa его даёт).
- Сетки подборок — свой экран для коллекций TMDB, списков и Кинопоиска (сортировка, догрузка
  страниц) и штатный `category_full` там, где он справляется сам.

**Фишки фазы 3**

- **Рулетка «Что посмотреть»** — пункт левого меню, кнопка «Что посмотреть» в шапке хаба и
  «Крутить по этой подборке» в сетке подборки (рулетка открывается с этой подборкой уже
  отмеченной): отдельные барабаны для фильмов и сериалов, выбор подборок чипами, фильтры
  «Не смотрел» и «Есть 90 минут» / «Серия до 30 минут». В стиле Apple TV (плоский вид) экран
  другой: мягкий фон из кадра главного фильма выборки, слева крупно — сколько в выборке, название,
  описание и кнопка «Крутить», справа барабан-кадр 16:9, под ними полка «Ещё в выборке» из пяти
  карточек 16:9 с кадром и логотипом названия (OK открывает фильм); результат — кадр во весь экран
  с логотипом названия и описанием.
- **Заставка из кадров** — замена штатной заставки Lampa: после нескольких минут без пульта экран
  сменяется полноразмерными кадрами известных фильмов с названием и часами; любое нажатие возвращает
  экран, и первое нажатие фокус не двигает. По умолчанию выключена — показывает заставка Lampa.
- **Меню по удержанию OK** — в штатное меню карточки дописываются «Трейлер», «Похожие», «Вся
  франшиза», отметка просмотра и «Скрыть из рекомендаций».
- **Ускорители пульта** — мини-карта рядов при удержании «вверх»/«вниз», листание ряда вдвое
  быстрее при удержании «влево»/«вправо», прыжок на десять карточек по кнопкам каналов.
- **Автоопределение слабого устройства** — плагин замеряет, за сколько телевизор успевает отрисовать
  первые карточки, и сам переходит на лёгкие анимации.
- **Девять акцентов, две тёмные темы, четыре ступени масштаба** — интерфейс подгоняется под свой
  экран и своё расстояние до дивана.

```
lumen-card/
├── src/                  ← исходники плагина по модулям (00_head … 99_tail), строгий ES5
├── dist/lumen_card.js    ← собранный плагин (результат scripts/build.mjs, коммитится)
├── scripts/              ← build.mjs (сборка), es5check.mjs (ES5-линт на acorn), chunks.mjs (инжект-чанки)
│   └── lib/acorn.mjs     ← вендоренный парсер acorn 8.14.0 (MIT), используется только es5check.mjs
├── test/                 ← node --test: юнит-тесты модулей (более 1400 тестов)
├── manifest.json         ← каталог подборок, тем и кадров заставки (генерируется сборкой)
├── lumen.js              ← короткий загрузчик для установки плагина по одной ссылке
├── harness/index.html    ← локальный тест-стенд с эмуляцией Lampa API
├── docs/tv-checklist.md  ← что проверить на живом телевизоре
└── README.md
```

---

## Установка

**Готовая сборка** (ветка `feat/lumen-v2`, обновляется сама через 1–2 минуты после каждого push):
в Lampa **Настройки → Расширения → Добавить плагин** → `https://azarkostya.github.io/lumen-card/lumen.js`
→ перезапустить Lampa. `lumen.js` — короткий загрузчик, который подключает лежащий рядом
`dist/lumen_card.js`. Для GitHub Pages в корне репозитория нужен пустой `.nojekyll` (без него сборка
Pages падает и адрес отдаёт 404). Ссылки `raw.githubusercontent.com` не подходят: они отдаются с
`nosniff`, и Lampa не выполнит скрипт.

**Свой хостинг:**

1. Выложите `dist/lumen_card.js` на любой **https**-хостинг (Lampa не грузит плагины по http, если
   сама открыта по https). Проще всего:
   - **GitHub Pages** — положите файл в репозиторий, включите Pages, ссылка будет вида
     `https://<user>.github.io/<repo>/dist/lumen_card.js`;
   - Cloudflare Pages / Netlify / любой статический хостинг;
   - собственный сервер с валидным сертификатом.
2. В Lampa: **Настройки → Расширения → Добавить плагин** → вставьте URL → **Добавить**.
3. Перезапустите Lampa (или откройте любую карточку).

После установки в **Настройках** появляется раздел **Lumen Card**, а в левом меню — пункты
**«Подборки»** и **«Что посмотреть»**.

> Плагин активируется только на «широкой» раскладке (TV / десктоп, ширина окна > 480 px).
> На телефоне в вертикальной раскладке он не вмешивается и карточка остаётся стандартной.

---

## Настройки (Настройки → Lumen Card)

Пунктов 46, поэтому они разложены по одиннадцати группам — заголовки групп это штатный параметр Lampa
`type: 'title'`. Группа отвечает на вопрос «про что это»: готовый стиль · вид · движение · фон карточки · блоки
карточки · главная · ряды подборок · пульт · рулетка · заставка · путь до плеера. Внутри группы не больше девяти
пунктов — столько строк раздела видно на экране телевизора без прокрутки. Порядок групп — от того,
что меняют чаще, к тому, что настраивают один раз. Вся раскладка задана таблицей `LC.prefs.LIST`
(`src/81_prefs.js`), из неё же собирается раздел.

| Параметр | Значения | По умолчанию | Что делает |
|---|---|---|---|
| **Включить Lumen Card** | вкл / выкл | вкл | Главный выключатель. «Выкл» снимает оформление немедленно: восстанавливается оригинальный шаблон `full_start_new`, удаляются оба `<style>` и `<link>` на шрифты, снимаются классы `lumen-*` и маркеры меню, у всех карточек в DOM убираются дорисованные блоки, останавливаются слои фона, трейлеры, частицы, заставка и все подписки на клавиатуру. Штатная разметка Lampa возвращается при следующем **открытии** карточки |

### Готовый стиль

| Параметр | Значения | По умолчанию | Что делает |
|---|---|---|---|
| Применить стиль Apple TV | кнопка | — | Выставляет **десять** пунктов «Оформления» разом: тема «Глубокая чёрная», акцент «Графит», шрифт Inter, метки «В подписи», цвет постера «Только фон», плоский вид включён, блоки анализа Lampa скрыты, кадр над рядами «Крупный», логотип названия в кадре включён, акцент от постера включён. Последние три совпадают со значениями по умолчанию, но тоже переписываются: стиль — это полное состояние, а не разница, поэтому «Кадр над рядами → Выключен» кнопка вернёт к «Крупному» (об этом сказано и в описании самой кнопки). Меняется **только вид** — ключ API, масштаб, анимации, заставка, состав рядов и настройки самой Lampa не трогаются. Пишутся только отличия, каждое отдельным `Lampa.Storage.set` с `nolisten`, а применяется набор **одним проходом** (`LC.applyPresetChanges`): одна пересборка таблицы стилей вместо пяти. Подписи пунктов в открытом разделе обновляются на месте (`Lampa.Params.update`). Повторное нажатие ничего не переписывает; что именно изменилось, перечисляет уведомление |
| Вернуть стиль Lumen | кнопка | — | Те же десять пунктов — к значениям по умолчанию плагина (тёплая тёмная тема, песочный акцент, Golos Text, метки на постерах, полная подкраска, плоский вид выключен, блоки анализа Lampa показаны, крупный кадр, логотип названия в кадре включён, акцент от постера включён). Значения берутся из той же таблицы `LC.prefs.LIST`, из которой раздел рисует пункты, — второй копии чисел в коде нет |

### Оформление

| Параметр | Значения | По умолчанию | Что делает |
|---|---|---|---|
| Акцентный цвет | Песок · Медь · Вино · Гранат · Мята · Изумруд · Лёд · Лаванда · Графит | Песок | Цвет кнопок, колец фокуса, полос прогресса и подсветок. Смена меняет четвёрку токенов акцента разом (цвет, текст на акценте, кольцо, свечение) и в CSS карточки, и в CSS экранов TorrServer |
| Акцент от постера | вкл / выкл | вкл | Цвет берётся из постера открытого фильма, фон страницы получает его оттенок. На главной цвет меняется, когда фокус постоял на карточке 3 секунды, и **въезжает за 1.6 с** — четырьмя шагами по 400 мс, через приглушённый тон, если оттенки далеко друг от друга; в «Лёгких» и «Выкл» анимациях цвет ставится сразу. Тёмный цвет высветляется, чтобы подписи читались; постер, не отдавший пиксели, оставляет акцент из пункта выше. Что именно произошло с пикселями, видно в строке отладки (`tint ok / load / timer / cors / dim / off`) — её включает «Отладка: показать FPS» |
| Где виден цвет постера | Полная / Только фон | Полная | Докуда доходит цвет, взятый с постера. «Полная» — как было: фон рядов, затемнение кадра героя, градиенты на кромках области рядов и подложка карточки под фокусом. «Только фон» снимает подложку фокуса — единственное место, где подкраска заходит на элементы управления; сам фокус остаётся, карточка по-прежнему увеличивается на 10 %. При **выключенном** «Акценте от постера» пункт не действует вовсе: подложка фокуса красится тогда акцентом из настроек, и постер к ней отношения не имеет |
| Тема | Тёплая тёмная / Глубокая чёрная | Тёплая тёмная | Цвет тёмного фона и подложек. «Глубокая чёрная» — настоящий чёрный (`#000`) для OLED |
| Плотные подложки | вкл / выкл | выкл | Кнопки, чипы и подложки текста становятся сплошными, без просвечивающего кадра и размытия. Для телевизоров, где полупрозрачность мылит или тормозит |
| Плоский вид | вкл / выкл | выкл | Содержимое лежит прямо на фоне, а не в коробках — так устроен Apple TV. В карточке: панель «Подробно» без рамки и подложки, строкой фактов под описанием; счётчики разделов без плашек; плитки серий — кадр, под ним подпись; отзывы плоским списком. На пути TorrServer раздачи и файлы разделяются тонкими линиями вместо карточек. В сетке подборки и в хабе меняется немногое: снимается только подложка под плиткой, а она и так лежит под картинкой — видно её, пока не пришёл постер или кадр, и у карточек без картинки (замер на стенде: у плитки хаба фон `rgb(16,16,18)` в обычном виде и прозрачный в плоском, разница заметна на плитках без загруженного кадра). Фокус (инверсия и подложка) и кегли не меняются; включён в стиле Apple TV. Экран «Что посмотреть» в плоском виде собирается по-другому — как «Смотреть» в Apple TV: фон из кадра главного фильма выборки, крупная типографика слева, барабан-кадр 16:9 справа, полка из пяти карточек 16:9 с логотипами под ними; вид решается при открытии экрана |
| Масштаб интерфейса | Мельче (0.9) / Обычный (1.0) / Крупнее (1.1) / Ещё крупнее (1.2) | Обычный | Коэффициент размеров на всех экранах плагина. Одно исключение: при «Размере интерфейса: крупнее» у самой Lampa и «Кадре над рядами» в значении «Крупный» карточки рядов главной упираются в высоту экрана, и «Обычный», «Крупнее» и «Ещё крупнее» дают там одинаковые ряды (потолок `rowScaleCap`, `src/30_css.js`); на остальных экранах плагина масштаб действует целиком |
| Фирменные шрифты | вкл / выкл | вкл | Подключает выбранную ниже гарнитуру с Google Fonts. При выключении `<link>` удаляется и используется системный стек |
| Шрифт | Golos Text · Onest · Manrope · Inter · IBM Plex Sans | Golos Text | Одна гарнитура на весь плагин: ею набраны и заголовки, и текст, и цифры — таймкоды, проценты и счётчики. При выключенных фирменных шрифтах не действует |

### Движение и эффекты

| Параметр | Значения | По умолчанию | Что делает |
|---|---|---|---|
| Анимации | Авто / Полные / Лёгкие / Выкл | Авто | Появление контента, пружина фокуса, наезд Ken Burns, компактная шапка. «Авто» — «Лёгкие» на Tizen/webOS и на устройстве, которое не успевает рисовать полные (см. автоопределение ниже), иначе «Полные». Смена кадров и трейлеры (карточки и главной) работают во всех режимах, кроме «Выкл» (трейлеры на Tizen/webOS по умолчанию выключены — «Трейлер в фоне карточки → Авто»); в «Лёгких» кадр меняется резко, без перехода |
| Тяжёлые эффекты | вкл / выкл | **на телевизоре выкл**, в браузере вкл | Частицы, наезд на кадр, зум заставки и плавная смена кадров в карточке и на главной — одним тумблером. Сами кадры и трейлеры от него не зависят: без тумблера кадр меняется резко. Значение по умолчанию считается по платформе: на Android TV, Tizen и webOS эти эффекты стоят кадров и без спроса не включаются, в браузере на компьютере они бесплатны и остаются. Работают только при полных анимациях |
| Отладка: показать FPS | вкл / выкл | выкл | Счётчик кадров, длинные задачи, разрешение и режим анимаций в углу экрана. Нужен для проверки на телевизоре — по нему калибруются пороги автоопределения анимаций; там же видна строка состояния подкраски от постера. Кадры по опозданию считаются от обычного интервала кадра устройства `P` (медиана), рядом — среднее fps и p95 за пять секунд, задержка колбэка кадра (`lat95`) и долгие кадры (`loaf`), `pf` — предзагрузка соседей (в пути / в очереди / детали из памяти); как читать — `docs/tv-checklist.md`, раздел 14 |
| Отладка: тест производительности | кнопка | — | Около минуты гоняет главную по восьми стадиям — покой и листание в «Лёгких» и «Полных», частицы, смена кадров, подкраска, всё сразу — и показывает таблицу во весь экран до «Назад»: fps, p50/p95 кадра, пропуски, долгие кадры, задержку кадра, число анимаций, цену кадра частиц; под ней — самый долгий кадр каждой стадии по фазам (скрипты, rAF и события анимаций, стиль и отрисовка, прочее) и его главный скрипт. Запускается с главной; ваши настройки не меняются (подмены — только в памяти), любая кнопка прерывает тест и показывает набранное. Что прислать — `docs/tv-checklist.md`, раздел 15 |
| Атмосферы | Все / Только сезонные / Выключены | **Только сезонные** | Слой частиц под тему фильма: снег у рождественского кино, летучие мыши у хоррора, звёзды у фантастики, дождь у нуара, песок, пузыри, лепестки, искры, помехи. Тема определяется по ключевым словам. Праздничные темы — сцены (угли, мыши и дымка у Хэллоуина; снег в трёх планах, боке и мерцающая гирлянда у Рождества; сердца у Дня святого Валентина), цена кадра на стенде при троттлинге CPU 10× — 1.8 мс против 1.1 мс у прежнего снега. «Только сезонные» — праздничные темы и только в свой месяц. Не запускается при лёгких и выключенных анимациях; под трейлером встаёт на паузу |

### Фон карточки

| Параметр | Значения | По умолчанию | Что делает |
|---|---|---|---|
| Слайдшоу кадров | вкл / выкл | вкл | Смена кадров фона: кроссфейд 0.6 с при полных анимациях и «Тяжёлых эффектах», иначе резкая смена; при «Анимации → Выкл» кадр один. Пауза, когда карточка не на экране и под трейлером |
| Интервал смены кадров | 8 / 14 / 20 с | 14 с | Время показа одного кадра — в карточке и на главной; новый интервал применяется сразу |
| Трейлер в фоне карточки | Авто / Вкл / Выкл | Авто | Трейлер с YouTube без звука через 3 с после открытия карточки: шапка сжимается, появляются метка «ТРЕЙЛЕР · БЕЗ ЗВУКА» и кнопка «Стоп», слайдшоу на паузе. «Авто» — выключено на Tizen/webOS. Не запускается при «Анимации → Выкл» |

### Блоки карточки

| Параметр | Значения | По умолчанию | Что делает |
|---|---|---|---|
| Логотип названия в карточке | вкл / выкл | вкл | Название в шапке карточки показывается логотипом фильма с TMDB — тем же, что в кадре главной (язык интерфейса, затем английский). Пока логотип не загрузился, место под ним уже занято, а текст не показывается; не доехал за 0.6 с или его нет вовсе — обычное название, и до закрытия карточки оно не подменяется. Включён по умолчанию — это видимая часть оформления карточки; выключается здесь же: Настройки → Lumen Card → «Блоки карточки» → «Логотип названия в карточке». «Выкл» — всегда текст |
| Показывать «Продолжить» | вкл / выкл | вкл | Полоса с таймкодом и процентом, подпись «Продолжить S2 E3» на кнопке «Смотреть» и отметки просмотра в карточках серий |
| Отзывы Кинопоиска | вкл / выкл | вкл | Ряд отзывов зрителей в блоке описания: «КИНОПОИСК · N отзывов», тон (ПОЗИТИВНЫЙ / НЕЙТРАЛЬНЫЙ / НЕГАТИВНЫЙ), дата, «N полезно»; OK открывает окно с полным текстом. Без ключа вместо ряда — подсказка, где его взять |
| Текст отзывов в ряду | Только заголовки / С выдержкой | Только заголовки | «Только заголовки» — в ряду автор, оценка и заголовок, текст открывается по OK: случайный спойлер не попадётся на глаза. Спойлерные куски скрыты в обоих режимах и раскрываются кнопкой в окне отзыва |
| Ключ Kinopoisk API | строка | пусто | Ключ `kinopoiskapiunofficial.tech` — бесплатный, 500 запросов в день. Нужен для отзывов, рейтинга КП и подборок Кинопоиска. Значение, которое дала сама Lampa, всегда приоритетнее нашего |
| Подсказка про ключ | вкл / выкл | вкл | Напоминание «Ключ API не задан» в карточке и в подборках Кинопоиска. Его же убирает кнопка «Скрыть» прямо на экране — переключатель нужен, чтобы вернуть подсказку обратно |
| Скрывать блоки анализа Lampa | вкл / выкл | **выкл** | Убирает с карточки ряды «Метаданные» (Темп, Страх, Экшн…) и «Настроения» (проценты). Это блоки **самой Lampa**, не плагина: строки `title_metadata`/`title_moods`/`title_meta_*` — `app.min.js:49246-49258`, рендер `MetadataChart` (`:38200`) и `MetadataTags` (`:38272`), данные приносит `Api.sources.cub.metadataGet` и только для фильма (`params.method == 'movie'`, `:20160-20166`); «Метаданные» появляются при `data.metadata.status == 'completed'` (`:38842`), «Настроения» — ещё и только при языке интерфейса ru/uk/be (`:38848`). Пункта настроек у Lampa для них нет (есть только флаг сборки `lampa_settings.disable_features.metadata`, `:34364`, — это её настройка, не наша). Ряды не прячутся и не снимаются со сцены, а **не создаются**: на событии `full` типа `start` (`:38833-38840` — оно уходит ровно перед проверкой) плагин убирает `data.metadata`, и Lampa не кладёт эти ряды в свой список; для неё это штатное состояние — так же выглядят сериал, не-русский язык и недосчитанный анализ. Данные не остаются изменёнными: снятое возвращается на `complite` (`:38965`), после единственного чтения. Применяется **при следующем открытии карточки**. Два других пути отвергнуты живой проверкой: `display:none` на ряде уводил фокус внутрь скрытого ряда (`Controller.collectionSet` отбирает по `offsetParent` только при третьем аргументе `visible_only`, `:46453-46456`, а карточка зовёт его одним, `:39126`), а снятие узла оставляло ряд в `this.items` — шаг «вниз» отдавал управление его компоненту с пустым набором, и фокус пропадал совсем. Включён в стиле Apple TV |

### Главная и подборки

| Параметр | Значения | По умолчанию | Что делает |
|---|---|---|---|
| Кадр над рядами | Крупный / Средний / Компактный / Выключен | Крупный | Какую часть экрана герой отдаёт тексту над рядами. Сам кадр занимает весь экран и стоит на месте, а размер задаёт, где начинаются ряды: в стартовом состоянии на 66.7 / 56 / 45 % высоты, а когда фокус уходит в ряды, они поднимаются к 50 / 42 / 34 % и наезжают на кадр (`HERO_VH` и `ROWS_TOP_VH`, `src/30_css.js`). Высота задана долями экрана и от его сторон не зависит. «Выключен» отдаёт экран рядам целиком, над ними — строка чипов настроения |
| Что показывает кадр главной | Кадры и трейлер / Только кадры | Кадры и трейлер | Кадры фильма сменяют друг друга с тем же «Интервалом смены кадров», что в карточке; в смену не идут только кадры, явно отвергнутые на TMDB (с голосами и оценкой ниже 1), — кадры без голосов идут. Первый кадр строже: с голосами и не хуже половины оценки ключевого арта, а нет такого — ключевой арт; ключевой арт в смене — только если он и встал первым. Кадр один только у фильма, где других кадров нет или все отвергнуты. «Кадры и трейлер» — если фокус постоял на карточке, кадры сменяет беззвучный трейлер (пункт «Автотрейлер в кадре главной»; на Tizen/webOS по умолчанию трейлера нет), а когда он кончится, кадры идут дальше. «Только кадры» — трейлер не запускается. Пока фокус в рядах ниже первого или уже на другой карточке (до её показа), кадры не меняются; при «Анимации → Выкл» кадр один. Применяется сразу |
| Автотрейлер в кадре главной | вкл / выкл | вкл | Кадр над рядами сам сменяется беззвучным трейлером с YouTube, если фокус постоял на карточке 8 секунд. Переход на другую карточку ролик снимает, при листании он не запускается вовсе; поиск и «Расширения», открытые поверх, тоже снимают его — главной под ними не видно, — а после их закрытия ролик снова ждёт 8 секунд. Кадры главной сменяют друг друга, пока ролик не играет, и идут дальше, когда он кончился. Не запускается при «Анимации → Выкл», «Трейлер в фоне карточки → Выкл» (на Tizen/webOS — и «Авто») и при «Что показывает кадр главной → Только кадры» |
| Логотип названия в кадре | вкл / выкл | вкл | Название фильма в кадре над рядами показывается его фирменной надписью с TMDB, а не обычным заголовком. Детали и надписи соседних карточек подгружаются заранее, поэтому надпись встаёт сразу, одним выводом; не успела за 0.6 с — или её нет вовсе — обычный заголовок, и до ухода с карточки надписью он не подменяется. «Выкл» возвращает текст сразу и логотипы больше не запрашивает |
| Профили настроения | вкл / выкл | вкл | Строка чипов над рядами главной, когда «Кадр над рядами» выключен (при кадре чипов нет — меньше текста в кадре, подборки есть в хабе и меню). «Выкл» снимает её с открытой главной сразу |
| Персональные ряды | вкл / выкл | вкл | «Досмотреть», «Потому что вы смотрели», «Новые серии ваших сериалов», «Скоро на экранах». Данные берутся из `Lampa.Favorite`/`Lampa.Timeline` и никуда не отправляются |
| Начало главной | Подборки по очереди / Сначала «Досмотреть» | Подборки по очереди | «Подборки по очереди» — первые ряды меняются при каждом запуске Lampa (не чаще раза в 10 минут) и раз в 3 часа, при следующем построении главной; «Назад» из карточки и смена настроек порядок не меняют. Сверху — подборка из всего каталога (кроме Кинопоиска без ключа и сезонных не в свой месяц; набор по умолчанию выпадает вдвое чаще), не повторяющая лидеров двух прошлых раскладок; «Досмотреть» вторым, третьим — первый ряд Lampa, дальше «Новые серии», подборки, «Потому что вы смотрели» (один из пяти последних фильмов истории, заголовок — ровно он), «Скоро на экранах». Две подборки одной группы подряд не стоят, сезонная в свой месяц — одна на первых восьми местах: не первой — с 4-го по 8-е место, своё в каждой раскладке, первой — не чаще раза в три раскладки. Выбранные вручную ряды меняются местами, состав прежний. «Сначала «Досмотреть»» — личные ряды сверху, как до волны 4. План — `src/47_homeplan.js` |
| Какие ряды показывать | экран выбора | набор каталога | Кнопка открывает список всех подборок каталога с галочками: отмеченные стоят первыми, остальные разложены по группам. Выбор сохраняется по каждой галочке. Не отмечено ничего — показывается набор по умолчанию |
| Количество рядов | 10 / 15 / 25 | 10 | Сколько рядов подборок строится на главной. Каждый ряд — отдельный запрос к каталогу, поэтому меньшее число заметно ускоряет появление главной на слабом телевизоре. Персональные ряды считаются отдельно |
| Не повторять фильмы в рядах | вкл / выкл | вкл | Фильм показывается в первом ряду, где встретился, а из рядов ниже выпадает — чтобы одна и та же новинка не стояла и в «Сейчас смотрят», и в «В тренде». Ряд, из которого что-то выпало, не показывается вовсе, если в нём осталось меньше четырёх карточек — или меньше половины прежнего и они не заполняют ширину экрана (число карточек, входящих в ряд, меряется пробником: 8 на 960×540, 9 на ПК пользователя); ряды, отмеченные вручную в «Какие ряды показывать», по этому порогу не снимаются, а у персональных рядов состав не трогается совсем — они только пополняют список уже показанного. Исключение из «первого ряда»: начатые фильмы и сериалы остаются в «Досмотреть» и «Новых сериях» и уходят из подборок даже выше этих рядов (подборка-лидер стоит над «Досмотреть») |
| Постеры карточек | Как в Lampa / Английские / Без надписей | Как в Lampa | Откуда берётся обложка в рядах главной и в сетках подборок. «Как в Lampa» — та, что приходит с карточкой: ни одного лишнего запроса, всё ровно как было. «Английские» (значение `original`) — тот же список, запрошенный с `language=en`, сопоставление по `id`: это английская обложка, а не обложка на языке оригинала — у аниме и дорам тоже английская, если она есть на TMDB. Цена — запрос на каждую половину подборки (фильмы и сериалы отдельно), то есть один-два на ряд и на каждую страницу сетки: страница переспрашивается та же, что пришла (замер 2026-09-23: 12.4 КБ, 89 мс на запрос), английская обложка нашлась у 159 фильмов из 160. «Без надписей» — постер без текста на любом языке (`movie/{id}/images?include_image_language=null`, первый подходящий с `iso_639_1: null`): по запросу на карточку — 20 на ряд из одного списка, до 40 у рядов с фильмами и сериалами («Netflix: Комедии» — 39, «Звёздные войны» — 33), 152 на набор главной по умолчанию без подборки Кинопоиска (живые запросы 2026-09-23; замер скорости: 203 КБ и 470 мс на ряд из 20 при параллельных запросах, 3.9 с при последовательных). Ответы кладутся в кэш Lampa на 30 дней — платит за них первое открытие и новые фильмы; кэш работает, только пока включено «Кэширование запросов» самой Lampa (раздел «Остальное»), выключено — платит каждое открытие. Постер без языка есть у 140 фильмов из 160 (замер по восьми разнородным рядам). Обложка непривычной пропорции не подставляется: берётся только то, что укладывается в 0.64…0.75 при ячейке 2:3. Постер прижат к верху ячейки (`object-position: center top`), поэтому узкий теряет низ, а широкий — поля по бокам; нижняя граница стоит в пустой полосе данных 0.487…0.650, ниже которой только «обои» 0.486 (1440×2960), верхняя — по срезу с боков: 0.75 — это 11.1 % ширины. Источник Кинопоиска и ряд адвент-календаря режим «Английские» пропускают — переспросить их список на другом языке нечем; «Без надписей» работает и там. |
| Метки на постерах | На постере / В подписи / Не показывать | На постере | «Скоро · 17 дек», «Новинка», процент просмотра и «Новая серия» в рядах главной и сетках подборок. «На постере» — плашкой поверх обложки; «В подписи» — тем же текстом первой строкой подписи, перед годом и рейтингом («Новинка · 2026 · ★ 7.3»), обложка при этом остаётся чистой. Полоса прогресса у недосмотренного лежит на постере в обоих видах. Подпись шириной с карточку и обрезается многоточием — при длинной метке год с рейтингом срезается; у метки ряда («Новая серия · 12 сен») в этом виде отбрасывается дата, иначе она заняла бы строку целиком (замер на стенде 960×540@2, штатный масштаб: подписи доступно 109.0 CSS px, полной метке нужно 108.4, без даты — 66.1). Пока метки показаны, рейтинг стоит в подписи, а штатная плашка Lampa `.card__vote` скрыта; «Не показывать» возвращает её на постер. Сохранённое значение прежнего переключателя переводится в новое один раз при запуске (`'true'` → «На постере», `'false'` → «Не показывать») |
| Скрывать досмотренное | вкл / выкл | **выкл** | Убирает из рядов подборок то, что помечено просмотренным в Lampa или досмотрено больше чем на 95 % |
| Свой каталог подборок | строка | пусто | Адрес своего JSON-каталога. Пусто — каталог плагина из интернета (см. ниже). Смена адреса сбрасывает кэш и тут же загружает каталог заново |

### Навигация и пульт

| Параметр | Значения | По умолчанию | Что делает |
|---|---|---|---|
| Меню по удержанию OK | вкл / выкл | вкл | Удержание OK на постере открывает штатное меню Lampa, а плагин дописывает в него «Трейлер», «Похожие», «Вся франшиза», отметку просмотра и «Скрыть из рекомендаций». Обычное нажатие по-прежнему открывает карточку |
| Мини-карта рядов | вкл / выкл | вкл | Удержание «вверх»/«вниз» на главной (от 0.5 с) показывает справа список рядов с подсветкой текущего. Нажатия не перехватывает и снимается вместе со сменой экрана |
| Быстрое листание | вкл / выкл | вкл | Удержание «влево»/«вправо» разгоняет листание ряда вдвое, кнопки каналов (CH+/CH−) прыгают сразу на десять карточек, позиция «14 / 60» показывается внизу экрана |

#### Управление мышью

Плагин реагирует и на пульт, и на мышь: наведение курсора на карточку главной двигает кадр героя
и подсветку так же, как шаг пульта, а «Меню по удержанию OK» из таблицы выше открывается мышью
удержанием левой кнопки (800 мс) или правым кликом — оба пути шлёт сама Lampa, тем же событием
`hover:long`. В «Подборках» (хаб и сетка подборки) наведение страницу не прокручивает, как у
штатной сетки Lampa: листают колесом, а постеры, кадры плиток и следующая страница подборки
догружаются по прокрутке, и пришедшая страница экран назад не откатывает.

Это отдельная работа, а не «само получилось»: один и тот же перевод фокуса Lampa сопровождает
РАЗНЫМИ событиями. Пульт и любая программная навигация идут через `Controller.focus`, который
шлёт цели `hover:focus` (`app.min.js:46438`); мышь идёт через `elem.trigger_mouseenter`, который
ставит цели класс `focus` и шлёт `hover:hover`, а `hover:focus` не шлёт вовсе
(`app.min.js:46360-46364`). Подписка на одно имя молчала бы ровно в половине случаев.

Поэтому ни один модуль плагина не подписывается на эти события сам — все идут через общий
`LC.focus` (`src/11_focus.js`), и за тем, что имена событий не расползлись по `src/`, следит
тест-сторож (`test/focus.test.mjs`). Мышиный режим управления включается в самой Lampa
(Настройки → Интерфейс → Тип управления); `hover:hover` приходит только в нём
(`app.min.js:46374-46381`).

### Рулетка «Что посмотреть»

| Параметр | Значения | По умолчанию | Что делает |
|---|---|---|---|
| Рулетка: только непросмотренное | вкл / выкл | вкл | С каким состоянием фильтра «Не смотрел» открывается рулетка. Сам фильтр на экране рулетки переключается чипом. Просмотренным считается помеченное в Lampa или досмотренное до конца |

### Экранная заставка

| Параметр | Значения | По умолчанию | Что делает |
|---|---|---|---|
| Заставка из кадров | вкл / выкл | **выкл** | Заменяет заставку Lampa: когда пульт молчит, экран сменяется полноразмерными кадрами с названием и часами. Работает, только пока собственная заставка Lampa выключена в её настройках, — двух заставок разом не бывает. Любое нажатие возвращает экран, и первое нажатие фокус не двигает. Не включается при играющем трейлере, открытом плеере или меню, в неактивной вкладке и при выключенных анимациях |
| Какие кадры | Известные фильмы / Кадры открытого фильма | Известные фильмы | «Известные фильмы» — отобранный список из каталога плагина (82 кадра), он обновляется вместе с каталогом. «Кадры открытого фильма» показывает кадры оставшейся на экране карточки и падает на отобранный список, если карточки нет |
| Через сколько включать | 3 / 5 / 10 мин | 3 мин | Сколько пульт должен молчать. Отсчёт начинается заново от любого нажатия |

### Меню и экраны плеера

| Параметр | Значения | По умолчанию | Что делает |
|---|---|---|---|
| Оформление меню и окон | Все меню и окна / Только путь до плеера / Выкл | Все меню и окна | Какие меню (`Select`) и окна (`Modal`) Lampa получают стиль Lumen. «Только путь до плеера» — выбор озвучки, качества, серии и раздачи. Меняется только вид: пункты, порядок и поведение окон остаются штатными |
| Оформление экрана торрентов | вкл / выкл | вкл | Стиль экранов пути через TorrServer: список раздач, окна подключения и ошибок, списки файлов, предзагрузка |

### Как настройки применяются

**Все настройки применяются сразу, без перезапуска Lampa и без повторного открытия карточки.** Это
обязательное требование, а не удобство: настройки Lampa открываются *поверх* карточки, и возврат из
них не шлёт ни события `full`, ни `complite` — значит `decorate` не перезапускается, и без
собственной точки применения на открытой карточке остались бы старые классы, узлы и CSS-переменные.
Поэтому у каждого пункта есть своя ветка в `applyPrefChange` (`80_settings.js`) и своя функция
`LC.apply*Pref` (`90_runtime.js`); что ни один пункт не остался без ветки, проверяет тест
(`test/settings.test.mjs`).

Два пункта по своей природе действуют со следующего **действия**, а не немедленно: включение плагина
(разметку строит Lampa по шаблону, а возврат «назад» карточку не перестраивает) и включение трейлера
(его отсчёт 3 с идёт от `complite`). Выключение обоих действует немедленно.

Настройки главной — особый случай: ряды живут в `Lampa.ContentRows`, и перерегистрация меняет то,
что Lampa построит **в следующий раз**. Поэтому после неё плагин пересобирает сам экран
(`Lampa.Activity.replace()` — та же активность с тем же объектом). Пока открыт слой настроек,
пересборка откладывается: замена активности закрыла бы раздел и увела фокус из списка, где
пользователь как раз крутит переключатели. Экран пересобирается в момент выхода из настроек
(событие `close` у `Lampa.Settings.listener`), а если пользователь ушёл вглубь — при возврате на
этот экран.

Lampa при записи настройки сначала рассылает событие `change` подписчикам `Storage`, а затем зовёт
`onChange` самого параметра. Чтобы настройка не применилась дважды, обработчик события помечает его
обработанным, а `onChange` эту пометку снимает и пропускает ровно одно своё событие. Пометка
ставится **только по факту применения**: `Subscribe.send` оборачивает весь цикл подписчиков в один
`try/catch`, и если чужой плагин, подписанный на `change`, бросит исключение раньше нас, наш
обработчик события не получит — тогда сработает `onChange`, и настройка всё равно применится.

Значения хранятся в `Lampa.Storage` под ключами `lumen_enabled`, `lumen_card_accent`,
`lumen_accent_auto`, `lumen_accent_scope`, `lumen_theme`, `lumen_solid`, `lumen_scale`, `lumen_card_fonts`, `lumen_font`,
`lumen_motion`, `lumen_fx`, `lumen_slideshow`, `lumen_slide_interval`,
`lumen_trailer`, `lumen_card_progress`, `lumen_reviews`, `lumen_reviews_mode`, `lumen_kp_key`,
`lumen_kp_hint`, `lumen_hero_size`, `lumen_hero_trailer`, `lumen_moods`, `lumen_personal_rows`, `lumen_home_start`,
`lumen_home_rows`, `lumen_rows_limit`, `lumen_badges`, `lumen_hide_watched`, `lumen_manifest_url`,
`lumen_context_menu`, `lumen_minimap`, `lumen_fastscroll`, `lumen_roulette_unseen`, `lumen_ambient`,
`lumen_ambient_source`, `lumen_ambient_delay`, `lumen_menus`, `lumen_torrents`. **Префикса
`lumen_card_` нет у большинства из них** — `applyPrefChange` разбирает такие ключи отдельными
ветками, до общего фильтра по префиксу. Булевы значения Lampa хранит строками `'true'`/`'false'`
(`Storage.set(name, false)` с JS-`false` не сохраняется вовсе). `lumen_home_rows` — строка с id
подборок через запятую. Кнопки (`lumen_home_rows` открывает экран выбора рядов, `lumen_preset_appletv`
и `lumen_preset_lumen` выставляют готовый стиль) своего значения не хранят.

Сменивший тип пункт переводится на новые значения один раз при запуске (`LC.migratePrefs`,
`src/81_prefs.js`) — через `Lampa.Storage.set`, то есть штатным путём Lampa, а не правкой
`localStorage` мимо неё: на записи висит её собственный `listener 'change'`, и у `Storage` есть свой
кэш значений в памяти. Сейчас так переводится `lumen_badges`: прежний переключатель хранил
`'true'`/`'false'`, новый список — `'poster'`/`'caption'`/`'off'`. Тот же разбор работает и при
чтении, поэтому профиль, до которого миграция ещё не дошла, выглядит правильно.

Кроме настроек плагин держит в `Lampa.Storage` кэши и служебные значения: `lumen_manifest` — каталог
с хостинга (12 ч), `lumen_kp_<подборка>_<страница>` — страницы Кинопоиска (30 дней),
`lumen_rv_<imdbId>` — отзывы (24 ч), `lumen_roulette_movie` / `lumen_roulette_tv` — выбранные в
рулетке подборки, `lumen_motion_auto` — вердикт автоопределения слабого устройства,
`lumen_motion_noty` — признак того, что о переходе на лёгкие анимации уже сообщали, `lumen_home_epoch` и `lumen_home_leads` — эпоха раскладки главной и её недавние лидеры.

---

## Каталог подборок

Из чего состоят ряды главной, хаб, сетки, темы атмосфер и кадры заставки, описано не в коде, а в
JSON-каталоге: **165 подборок, 10 групп, 4 профиля настроения, 9 тем, 82 кадра заставки**. Формат
подборки — id, заголовок (с `i18n` для en/uk), группа, необязательные `badge` и `season` (месяцы,
когда подборку поднимают наверх) и `sources`: описание запроса для `movie` и/или `tv` (`discover` с
параметрами TMDB, `collection`, `list` или `kp`). Сезонные подборки сейчас такие: рождественские и
новогодние (декабрь–январь), «Хэллоуин» (сентябрь–ноябрь), «Кино о любви» (февраль) и «Кино о
войне» (май).

- **Ток-шоу и подкасты в сериальных подборках не показываются.** Признак — жанр TMDB 10767
  («Talk») без 10764 («Reality»): реалити с обоими жанрами (Top Gear, The Grand Tour, Single's
  Inferno) остаются. В рядах, сетке подборки и рулетке это отсечение ответа
  (`talkOnly`, `src/43_sources.js`), поэтому страница может быть на карточку-две короче; в полной
  сетке Lampa (`category_full`) ответ фильтровать нечем, и там запрет стоит в самом запросе —
  реалити с жанром 10767 туда не попадают.
- **Каталог обновляется сам.** Плагин загружает его с
  `https://azarkostya.github.io/lumen-card/manifest.json` (тот же репозиторий, ветка
  `feat/lumen-v2`), кэширует ответ на 12 часов и дальше берёт из кэша. Новые подборки, темы и кадры
  заставки приезжают без переустановки плагина.
- **Без интернета работает встроенный список** — та же копия каталога зашита в плагин и
  используется, когда сети нет или ответ не похож на каталог.
- **Битый ответ не портит рабочий кэш**: если пришло что-то, не проходящее проверку, плагин
  оставляет прежний каталог (или встроенный, если кэша ещё не было).
- **Свой каталог**: выложите JSON того же формата на любой https-адрес и впишите его в
  **Настройки → Lumen Card → Свой каталог подборок**. Эта настройка приоритетнее адреса по
  умолчанию; смена адреса сбрасывает кэш и загружает каталог заново, не дожидаясь 12 часов.
  Образец — `manifest.json` в корне репозитория (его генерирует `scripts/build.mjs` из
  `src/42_manifest.js`, поэтому файл и код не расходятся).

**Как добавить свою тему атмосферы.** Запись в массиве `themes`:

```json
{"id": "western", "preset": "sand", "accent": "#D8A76A",
 "keywords": ["western", "cowboy", "wild west"], "genres": [37],
 "requireGenre": false, "months": []}
```

`preset` — один из двенадцати движков частиц: девять простых (`bats`, `snow`, `stars`, `rain`,
`sand`, `bubbles`, `petals`, `embers`, `glitch`) и три праздничные сцены (`halloween`, `winter`,
`hearts`) со свечением и несколькими планами; `keywords` сравниваются по вхождению, без учёта регистра, с
английскими ключевыми словами фильма из TMDB; `requireGenre: true` требует ещё и совпадения жанра;
`months` — месяцы, в которые тема считается сезонной (в режиме «Только сезонные» показываются лишь
такие темы и только в свой месяц). Побеждает первое совпавшее правило по порядку массива.

**Как добавить кадр в заставку.** Запись в массиве `ambient`:

```json
{"media": "movie", "id": 693134, "title": "Дюна: Часть вторая",
 "path": "/eZ239CUp1d6OryZEBPnO2n87gMG.jpg", "width": 3840}
```

`path` — путь кадра в TMDB (`backdrops` без языка, чтобы на картинке не было надписей); `width`
берётся оттуда же и нужен, чтобы плагин выбрал размер под экран (`w1280` на HD-панелях,
`original` на 4K).

---

## Путь до плеера (TorrServer)

Все экраны от кнопки «Смотреть» до плеера оформлены в стиле карточки, при этом ни одна строка
логики и разметки Lampa не меняется — только CSS и классы-маркеры:

| Экран | Что оформлено |
|---|---|
| «Источник» | Панель выбора: заголовок, пункты, фокус инверсией (светлая заливка, тёмная подпись). Пункты и иконки сторонних плагинов (онлайн-балансеры, реклама) не перекрашиваются — **белый логотип балансёра на светлой заливке фокуса не виден**, см. ниже |
| «Торренты» | Карточка фильма слева, чипы «Назад / Поиск / Сортировать / Фильтр» (точка на «Фильтре», если он применён), раздачи с моно-метаданными, светлым чипом размера, медиачипами и кольцом фокуса; «Сортировать», «Фильтр», меню действий над раздачей |
| Подключение | Окно ожидания TorrServer с пульсирующей иконкой; «Необходим TorrServer» |
| Ошибки | Чек-лист диагностики (пройденные / текущий / будущие шаги), «Не удалось получить хэш», таймаут |
| Файлы | Список файлов фильма и серий сериала (превью, прогресс просмотра, папки), меню действий над файлом |
| Предзагрузка | Экран буферизации перед плеером: название, прогресс, пиры |

Как это устроено: `src/64_menus.js` (`LC.menus`) по содержимому вызова ставит маркер `lumen-select`
с `data-lumen-kind` (`source` / `torrent` / `file` / `filter`) на корень `.selectbox` и `lumen-modal` на
окна TorrServer (`.modal-loading`, `.torrent-install`); `src/65_torrents.js` (`LC.torrents`) держит все
правила пути в одном `<style id="lumen-torrents-css">` и включает их классом `lumen-torrents-on` на
`body`. Режим «Все меню и окна» оформляет и остальные меню Lampa (настройки, плеер); «Только путь до
плеера» — только помеченные; «Выкл» — ничего.

> **Android TV:** экраны подключения, ошибок и списка файлов Lampa показывает только при включённом
> **Настройки → TorrServer → Встроенный клиент** (`internal_torrclient`, на Android по умолчанию
> выключен — без него Lampa сразу передаёт раздачу внешнему приложению TorrServer).

Что не сделано осознанно (потребовало бы менять разметку Lampa): подписи «Подключаемся к TorrServer» и
«Назад — отменить», текст «ПРОДОЛЖИТЬ» у строки продолжения (вместо него иконка), приглушение
просмотренных серий; иконки-спрайты Lampa в меню не заменяются маской Lumen (без `:has()` их нельзя
отличить от чужих) — только размер и цвет.

Прямое следствие этого — **светлый логотип стороннего балансёра пропадает под фокусом**. Фокус
пункта меню и фокус кнопок с Task 54 — инверсия: заливка становится цветом текста темы, то есть
светлой. Цвет иконке плагин не задаёт ни своей, ни чужой: как она покрасится, решает её собственная
разметка: Lampa подставляет в шаблон пункта готовый `{icon}` целиком, и что там за `svg`, зависит
от того, кто открыл меню. Иконка, нарисованная под тёмный фон (белая заливка в самой разметке), на
светлой заливке исчезает. Перекрасить только чужую нечем: без `:has()` её не отличить от штатной, а
красить все подряд — значит потерять цветные логотипы. Подпись пункта читается всегда: пропадает
именно картинка слева. Что проверить на живом экране — `docs/tv-checklist.md`, раздел 8.

---

## Главная, хаб, сетки и рулетка

| Элемент | Как устроен |
|---|---|
| Герой | Два соседних узла в корне главной, оба перед рядами. `.lumen-hero-stage` — неподвижный слой кадра на весь экран, без `transform`: кадр `<img decoding="async" fetchpriority="high">` с `object-fit: cover; object-position: center 25%`, подложка, ролик и затемнение — три градиента тенью фона страницы (фон, смешанный с чёрным наполовину: подкраска от постера красит и их, а мета на белом кадре держит 4.5:1 и с самой светлой подкраской; низ и пол к сплошной части приходят самим фоном рядов): `.lumen-hero__scrim--l` (слева под текстом, от .95 до нуля ровно у правой кромки, .84 до 56 % ширины — под метой при любом «Размере интерфейса» Lampa, без полки и границы; в разметке — под остальными, чтобы не темнить фон рядов), `.lumen-hero__scrim` (верх под шапкой и низ, растворяющий кадр в рядах) и `.lumen-hero__floor` — пол под поднятыми рядами. `.lumen-hero` — логотип, мета (не больше двух жанров) и описание. Карточку под фокусом ловит слушатель `hover:focus` (пульт) и `hover:hover` (мышь) на корне главной (данные лежат в `el.card_data`), детали, логотип, ключевые слова и кадры фильма приходят одним запросом `Lampa.Api.sources.tmdb.get(media + '/' + id, …, {life: 1440})`. Этот же запрос и логотип соседей заранее делает `LC.prefetch` (`src/58_prefetch.js`): через 0.25 с покоя фокуса — соседи по направлению листания (+2/−1, в «Полном» +3/−2) и три первые карточки следующего ряда, не больше двух запросов разом, при зажатой стрелке — ни одного; после первого показа — первый экран главной; детали из его памяти (40 фильмов) герой берёт синхронно, логотипы держатся загруженными (24 картинки, 16 МБ растра), кадры `w1280` заранее не грузятся. Кадр героя — первый кадр фильма без надписей из `images.backdrops` (16:9, от 1280 px, с голосами TMDB и оценкой не ниже половины оценки ключевого арта и не ниже 1 — явно отвергнутые кадры не берутся и без планки, не сам ключевой арт `backdrop_path`, который часто повторяет постер в ряду; `heroBackdrop`), деталей он ждёт до 900 мс, дальше — `backdrop_path`. Обновление с задержкой 350 мс, чтобы быстрое листание не дёргало картинку; кадр прошлого фильма под текстом нового держится не дольше 250 мс — дальше постер карточки из ряда (обычно уже в кэше браузера, у только что въехавшей в ряд карточки может ещё грузиться) до прихода кадра, а без постера нейтральный фон. Заглушка встаёт только у карточки, на которой остановился фокус: при листании постеры-заглушки не вспыхивают, а кадры пройденных карточек могут смениться, если листать по одному нажатию (шаг длиннее 0.35 с), — каждая пройденная карточка успевает показаться; при удержании стрелки шаг короче, и пройденные карточки не показываются вовсе; байты нового кадра доехали и он декодируется — ещё 150 мс. До первого показанного кадра под ним лежит тот же кадр в `w300` (0.2 МБ растра), и через 0.9 с после появления `w1280` эта подложка снимается. После 8 с покоя фокуса в кадре запускается трейлер: бокс ролика — во весь экран, а его `iframe` на 10 % больше со всех сторон (заголовок YouTube — за кромкой экрана; чёрная полоса широкоформатного ролика 2.39:1 сверху сократилась с 69 до 29 px на 960×540 и лежит в полосе шапки Lampa под верхним затемнением .5 — приглушена, но видна: шапка прозрачна). При уходе фокуса ниже первого ряда текст получает `.lumen-hero--compact` и уезжает вверх, корень — `.lumen-rows-up`: ряды поднимаются на неподвижный кадр, под ними проявляется пол |
| Чипы настроения | `.lumen-moods` собственным узлом корня главной — только когда кадр над рядами выключен (при живом кадре `LC.moods` их не ставит). Обычные `.selector`, поэтому пространственная навигация Lampa находит их сама. OK открывает штатную сетку `category_full` с параметрами профиля |
| Ряды подборок | `Lampa.ContentRows.add({name, title, screen:'main', index, call})`. Запрос уходит только когда Lampa действительно строит ряд; при уходе с главной незавершённые запросы отбрасываются по поколению. В декабре добавляется ряд «Адвент-календарь · день N»: детерминированный фильм на каждый день до сегодняшнего |
| Персональные ряды | «Досмотреть» — `Lampa.Favorite.continues()`, из которого выпадают фильмы, досмотренные до 90 % и больше (сериалы остаются: «последний доступный эпизод» по данным закладок не вычисляется). Это фильтр показа — ни `Favorite`, ни `Timeline` плагин не переписывает, и при повторном запуске карточка вернётся в ряд сама. «Потому что вы смотрели» — `…/recommendations` одного фильма из пяти последних в истории (он же в заголовке), «Новые серии» — детали сериалов из закладок и истории (серия вышла ≤ 14 дней назад или выйдет ≤ 7 дней), «Скоро на экранах» — `discover` на 30 дней вперёд |
| Метки на постерах | Один `MutationObserver` на активность главной: у добавленных `.card` считается метка по `card_data` и вставляется внутрь `.card__view`, не трогая штатные `.card__quality`/`.card__type`. Наблюдатель снимается вместе с уходом с экрана |
| Хаб «Подборки» | Свой компонент `lumen_hub` плюс пункт в левом меню. Чипы групп, плитки с коллажом из трёх постеров (грузятся для видимых плиток и по фокусу), поиск по названию через `Lampa.Input.edit` |
| Сетка подборки | Свой компонент `lumen_grid` для коллекций TMDB, списков и Кинопоиска: сортировка «По популярности / По рейтингу / Новые», догрузка страниц. Для подборок, которые целиком описываются одним `discover`, открывается штатный `category_full` — у него своя пагинация и фильтры |
| Рулетка | Свой компонент `lumen_roulette` плюс пункт левого меню «Что посмотреть». Пул кандидатов собирается из выбранных подборок (первые две страницы, из общего кэша источников), фильтруется по «не смотрел» и длительности, барабан крутится 3.1 с. Результат — кадр, название, мета и кнопки «Смотреть», «Ещё раз», «В закладки» (логотипа у результата нет: деталей с логотипами у карточек выборки нет, это был бы отдельный запрос на каждый результат). Входы — пункт меню, кнопка в шапке хаба и кнопка сетки подборки (с `preselect`) |
| Кинопоиск | `films/collections?type=…` (заголовок `X-API-KEY`), затем `find/{imdbId}` в TMDB для каждой позиции — до 20 запросов на страницу; результат кэшируется на 30 дней. Без ключа запросов нет вовсе: плитка помечается «НУЖЕН КЛЮЧ», сетка показывает подсказку |

---

## Что рисуется и откуда берутся данные

| Элемент | Источник |
|---|---|
| Бэкдроп | `Lampa.Api.img(movie.backdrop_path, 'w1280')`, фолбэк `movie.background_image`. Если ничего нет или картинка не загрузилась — процедурный CSS-градиент (3 варианта, выбор по `movie.id % 3`) |
| Мета-строка | год из `release_date`/`first_air_date`, страны из `Lampa.TMDB.parseCountries()`, хронометраж из `runtime`, для сериала — `number_of_seasons`/`number_of_episodes`, качество из `release_quality`/`quality`, жанры из `genres`, возраст из `Lampa.TMDB.parsePG()` |
| Таблица «ПОДРОБНО» | Только то, чего нет в мета-строке: оригинальное название, полная дата премьеры, создатель сериала (`created_by`), бюджет фильма (`budget`) |
| Заголовок | `{title}` из шаблона. Логотип фильма (`images.logos` из ответа `Api.full`, своего запроса к TMDB нет; язык интерфейса → английский → без языка) — и в герое главной, и в шапке карточки (настройка «Логотип названия в карточке»). Адрес логотипа у обоих экранов один, поэтому при переходе с главной он берётся из кэша браузера. Пока исход логотипа неизвестен, заголовок скрыт, но не дольше 0.6 с (`TITLE_WAIT`, `src/48_hero.js`); не доехал — текст до конца показа, поздний логотип его не подменяет. Нет логотипа — двухуровневое название текстом |
| Описание | Ряд описания Lampa целиком (`movie.overview`) — в шапке карточки его нет, чтобы один текст не стоял на экране дважды |
| Рейтинги | `.rate--tmdb` / `.rate--imdb` / `.rate--kp` — заполняет сам движок Lampa. IMDb и КП показываются, только если Lampa их прислала (либо КП приходит вместе с отзывами по ключу) |
| «Продолжить» | `Lampa.Timeline.view(Lampa.Utils.hash(...))`. Для фильма — хэш `original_title`. Для сериала перебираются сезоны 1…`number_of_seasons` (макс. 10) × серии 1…30 и выбирается запись с самым свежим `updated` |
| Франшиза | `collection/{id}` по `belongs_to_collection` (кэш 7 дней): номер части, отметка просмотренного, «Вы здесь», «Дальше» |
| Ключевые слова (темы атмосфер) | `keywords.results` (movie) / `keywords` (tv) из того же запроса деталей, что и остальное |
| Акцент от постера | `Lampa.TMDB.image('t/p/w185' + poster_path)` (с `crossOrigin="anonymous"`, поставленным до `src`) → `canvas 16×16` → двенадцать корзин оттенка, побеждает самая весомая. Не отдал пикселей — тихий фолбэк на акцент из настроек; чем именно кончилась попытка, показывает строка отладки. Запрос без ответа отпускается через 6 с. Прокси TMDB без `Access-Control-Allow-Origin` при `crossOrigin="anonymous"` даёт не `SecurityError`, а ошибку загрузки — тогда идёт вторая попытка прямым адресом `image.tmdb.org` |
| Отзывы Кинопоиска | `kinopoiskapiunofficial.tech` с ключом пользователя (заголовок `X-API-KEY`): `films?imdbId=…` → `kinopoiskId`, затем `films/{id}/reviews`. До 12 отзывов, число в заголовке — `total` ответа. Кэш 24 ч (ключ `lumen_rv_<imdbId>`), «отзывов нет» — 2 ч |
| Кадры заставки | Каталог плагина (`ambient`) или кадры открытой карточки. Размер выбирается по ширине экрана в физических пикселях: `original` только выше Full HD (`w1280`, пока ширина × 0.85 не превысила 1920), тот же порог, что у кадра карточки и кадра героя |

**Никаких выдуманных данных.** Чипы, которых нет в дизайн-макете как реальных полей
(«СМОТРЯТ 4.1K», «HDR10», «ATMOS», «RUS · ENG»), не рисуются — у Lampa нет для них источника.

---

## Совместимость

- **JS**: строгий ES5 — `var`/`function`, без стрелок, `let`/`const`, шаблонных строк, классов,
  spread, `Object.assign`, `Array.prototype.find`/`includes`. Работает на webkit Smart TV 2016 г.
  и Android 4.1 webview.
- **CSS**: без CSS-переменных в качестве механизма темы — акцент подставляется в CSS **литералом**
  при генерации `<style id="lumen-card-css">`, поэтому тема работает даже там, где `var()` не
  поддерживается. Флексбокс продублирован префиксами `-webkit-box` / `-webkit-flex`. Не
  используются `inset` и `:has()` — их нет в браузерах телевизоров 2016–2019 годов.
  Два исключения у кадра героя: `object-fit` (есть с Chrome 32 и Android WebView 4.4.3 — на
  webview Android 4.1 кадр растянется без обрезки) и `-webkit-mask-image` для вертикального
  затухания левого затемнения (на движке, который её не знает, затемнение слева пойдёт на всю
  высоту экрана: верх кадра слева станет темнее, но кромки не появится). На слое автотрейлера
  маски с волны 3 нет: низ ролика растворяет то же затемнение слоя кадра, что и статичный
  кадр, — обычные градиенты поверх `iframe` YouTube. На живом телевизоре это не проверялось —
  см. `docs/tv-checklist.md`, раздел 7, пункт 5.
- Единственная внешняя зависимость — опциональный `<link>` на Google Fonts (отключается в настройках).
- Весь код обёрнут в `try/catch` по ключевым местам: исключение в отрисовке не ломает карточку.
  Оригинальный шаблон сохраняется при старте и возвращается, если подмена шаблона упала.
- Все слои плагина (частицы, заставка, слой перехода рулетки, мини-карта) живут в собственных узлах и снимаются
  при смене экрана и при выключении плагина; ни один из них не перехватывает клавиши — единственное
  исключение — первое нажатие, закрывающее заставку.

### Что сохранено из оригинального шаблона `full_start_new`

Все узлы и классы, на которые опирается `src/components/full/start.js` и его модули:

`.full-start-new__title`, `__head`, `__details`, `__reactions`, `__tagline`/`.full--tagline`,
`__buttons`, `.buttons--container`, `.button--play`, `.button--book`, `.button--reaction`,
`.button--subscribe`, `.button--options`, `.view--torrent`, `.view--trailer`,
`.rate--tmdb`/`.rate--imdb`/`.rate--kp` с вложенными `div`, `.full-start__pg`,
`.full-start__status`, `.tag--year`, `.tag--time`, `.tag--quality`, `.tag--episode`,
`.is--serial`, `img.full--poster` внутри `.full-start-new__poster`, все языковые ключи `#{...}`.

SVG кнопок скопированы **побайтово** из оригинала — `start/buttons.js` хэширует `outerHTML`
кнопки для механизма «приоритетной кнопки», и любое изменение разметки сбросило бы сохранённый
приоритет.

Все кнопки остаются `.selector`, поэтому `Controller.collectionSet(this.html)` собирает их как
обычно и навигация пультом не ломается.

### Что скрыто (узлы остаются в DOM)

- `.full-start-new__left` с постером — в дизайне постера нет (но `img.full--poster` на месте,
  `poster.js` вешает на него `onload`);
- `.full-start-new__head` и `.full-start-new__details` — вместо них рисуется своя мета-строка
  (скрываются только после успешной сборки меты: если сборка упала, показывается оригинал);
- `.full-start-new__tagline` — нет в дизайне;
- `.full-start-new__reactions` — строка со счётчиками реакций; **кнопка** `.button--reaction`
  продолжает работать;
- `.full-start__pg` внутри строки рейтингов — возраст переехал в мета-строку;
- `.tag--year`, `.tag--time`, `.is--serial` — в служебном скрытом контейнере `.lumen-keep`;
- родной фон Lampa `.full-start__background` — получает класс `lumen-off`, вместо него рисуется
  свой полноширинный слой `.lumen-backdrop`.

---

## Ограничения и известные риски

### Главное, о чём стоит знать до установки

**На телевизоре проверено частично.** Разработка и приёмка идут на локальной сборке Lampa 3.3.4 в
браузере на компьютере, навигация — синтетическими нажатиями клавиш. На настоящем телевизоре плагин
работал у пользователя — Philips 50PUS8057, Android TV, WebView Chromium 77, окно 960×540 CSS px при
devicePixelRatio 2: по отзывам и фото с него (2026-09-18, 2026-09-21) сделаны фазы 4–6. На фото
строки отладки 2026-09-21 главная — 60 fps в покое и 36–55 при листании; карточка длинного сериала
проваливалась до 2–5 fps, ответ на это — ряд серий «окном» (Task 67), на телевизоре ещё не
проверенный (`docs/plans/2026-09-22-lumen-phase6-tv-feedback.md`, раздел 0). **Всё, что
сделано после фазы 6, на телевизоре ещё не проверялось**, а на Tizen и webOS не проверялось ничего.
В браузере нельзя проверить
главное: реальное воспроизведение трейлеров YouTube, подключение к TorrServer, скорость прокрутки
рядов и открытия карточки, читаемость с дивана и поведение физического пульта (удержание OK,
удержание стрелок, кнопки каналов). Что именно стоит проверить и что прислать для калибровки —
в `docs/tv-checklist.md`.

**Пороги автоопределения слабого устройства по телевизору не проверены.** Плагин замеряет время от
построения экрана до второго кадра отрисовки, берёт медиану трёх замеров и при 400 мс и выше
переходит на лёгкие анимации (ниже 250 мс — считает устройство быстрым; между — ничего не меняет;
`SLOW_MS`, `FAST_MS`, `src/68_perf.js`). Эти числа взяты из плана. На телевизоре пользователя
«Лёгкие» выбраны вручную, поэтому вердикта автоопределения там никто не видел, и калибровать пороги
пока не по чему: на другом телевизоре порог может оказаться и слишком строгим (лёгкие анимации там,
где хватило бы полных), и слишком мягким. Свои замеры видно
в консоли через `window.lumen_card.perf.samples()`; вердикт в любой момент перекрывается ручным
выбором в «Анимации».

Ни один вердикт не вечен: устройство перемеряется на первых трёх карточках **каждого** запуска.
Эти три карточки — самые холодные (пустой кэш постеров, неразобранный шрифт), поэтому один их
результат не имеет права решать за плагин навсегда: и «слабый» может оказаться случайным, и
«быстрый». Лёгкие анимации включаются сразу по плохому вердикту, а возвращаются полные только
после пяти хороших запусков подряд — переключение видно глазами, и дёргать его от запуска
к запуску плагин не должен. Измерение стоит два отложенных кадра на карточку, без сети и таймеров.
Замеряются три экрана: открытие карточки, главная (монтирование героя; не больше одного замера за
запуск — `MAIN_LIMIT`) и экран «Подборки»; в медиану идут первые три замера с любых из них.

**Ключ Кинопоиска хранится открытым текстом.** Он лежит в `Lampa.Storage` (localStorage
устройства) обычной строкой — его видит любой скрипт на той же странице, включая другие плагины
Lampa, и он попадает в бэкап/синхронизацию настроек, если она включена. Ключ уходит заголовком
`X-API-KEY` на сторонний домен `kinopoiskapiunofficial.tech` — это не сервис Lampa и не сервис
плагина. Бесплатный лимит — **500 запросов в сутки на ключ**, и подборки Кинопоиска тратят его
быстрее всего: одна страница — запрос в КП плюс до 20 запросов сопоставления в TMDB (кэш на
30 дней смягчает, но не отменяет). Если это неприемлемо — не задавайте ключ: без него отзывы,
рейтинг КП и подборки Кинопоиска не показываются, остальное работает.

**У части свежих фильмов название и описание будут английскими.** TMDB отдаёт перевод только там,
где его уже добавили волонтёры, и у только что вышедших или малоизвестных фильмов русского перевода
часто нет. Плагин показывает то, что пришло, и ничего не переводит сам — так же ведёт себя и штатная
карточка Lampa. Отдельно: ключевые слова, по которым выбирается тема атмосферы, у TMDB **всегда**
английские — на выбор темы язык интерфейса не влияет.

**Каталог подборок грузится с GitHub Pages** (`azarkostya.github.io`) — раз в 12 часов, без
параметров и без данных пользователя. Домен недоступен — работает встроенный каталог; адрес можно
заменить своим в настройках.

**Трейлеры работают только там, где работает YouTube.** И фоновый трейлер карточки, и трейлер в
кадре главной — это `iframe` с YouTube. На устройстве без доступа к YouTube (регион, провайдер,
отсутствие сервисов Google) они просто не запустятся; на Tizen/webOS оба по умолчанию выключены
(«Авто»), потому что там `iframe` поверх карточки стоит дороже выигрыша.

**Атмосферы — самая тяжёлая часть плагина.** Частицы рисуются на `canvas` (не больше 60
штук, плотность пикселей ограничена 1.5) и не запускаются при лёгких и выключенных анимациях, то
есть и на слабых телевизорах. Если картинка дёргается — начните с «Анимации → Лёгкие» и «Плотные
подложки → вкл».

**Герой по умолчанию отдаёт тексту две трети высоты экрана.** Кадр главной занимает весь экран и
не двигается; «Кадр над рядами → Крупный» (значение по умолчанию) задаёт, где начинаются ряды: в
покое на 66.67 % высоты, а когда фокус уходит в ряды, они поднимаются к 50 % и наезжают на кадр
(`HERO_VH` и `ROWS_TOP_VH`, `src/30_css.js`); «Средний» и «Компактный» — 56 и 45 %. Высота задана
долями экрана и от его сторон не зависит. В сетке подборки мини-героя
нет: её содержимое не прокручивается вместе с шапкой экрана, и герой занимал бы место постоянно.

**Netflix для фильмов — по региону US.** Провайдера Netflix у TMDB в регионе RU нет, поэтому
фильмы подборки берутся с `watch_region=US` и в вашей стране могут быть недоступны. Сериалы
Netflix идут по сети (`with_networks=213`) и от региона не зависят.

### Остальное

1. **IMDb и КП рейтинги.** Поля `movie.imdb_rating` / `movie.kp_rating` заполняются только при
   настроенном парсер-сервере или CUB-аккаунте. На «голом» TMDB их нет — чипы не появятся.
   Это не баг плагина: он лишь стилизует узлы, которые заполняет сама Lampa.
2. **Шрифты требуют интернет.** Google Fonts грузятся с `fonts.googleapis.com`. Если TV без
   доступа или провайдер режет домен — выключите «Фирменные шрифты», будет системный стек.
3. **Языки аудиодорожек / HDR / ATMOS** из дизайн-макета не рисуются — у Lampa нет для них
   источника на этапе карточки (данные появляются только после выбора источника в плеере).
4. **Прогресс сериала** ищется перебором до 10 сезонов × 30 серий (300 обращений к
   `Lampa.Timeline.view`). Если сезонов больше 10 или серия за номером > 30 — «Продолжить»
   не покажется. Перебор нужен потому, что `Timeline.watched()` в самой Lampa ищет только
   по первому сезону.
5. **Реакции.** Строка со счётчиками скрыта (нет в дизайне). Если она нужна — уберите
   `.full-start-new__reactions` из правила скрытия в `buildCss()`.
6. **«Скрыть из рекомендаций»** убирает фильм из рядов не сразу: ряд уже построен Lampa, и карточка
   исчезнет при следующем построении — об этом плагин и говорит в уведомлении.
7. `100vh` у слоя бэкдропа и `74vh` у тела карточки завязаны на высоту окна. На нестандартных
   webOS/Tizen-обёртках с плавающей высотой viewport фон может оказаться чуть выше/ниже.
   `74vh` снимается на узком экране — там шапка карточки схлопывается по содержимому. «Узкий»
   считается в физических пикселях: порог `max-width` равен `1000 / devicePixelRatio`, поэтому
   телевизор с окном 960 CSS px при DPR 2 (растр 1920) узким не считается и держит полную
   раскладку. Заголовок карточки в этой же ветке — ступенью ниже: Title 2 вместо Title 1.
8. Слой `.lumen-backdrop` вставляется `prepend`-ом в корень компонента (`e.body`) — ровно туда,
   куда Lampa кладёт свой `.full-start__background`, и позиционируется так же (`position:absolute`).
   Если в конкретной сборке ближайший позиционированный предок — скролл-контейнер, фон при
   прокрутке карточки может не уезжать вместе с контентом.
9. **Меню по удержанию OK** дописывает пункты в штатное меню Lampa, распознавая его по содержимому.
   В сборке, где это меню собрано иначе, пункты просто не добавятся — штатное меню при этом не
   пострадает.
10. **Переход компактной шапки** анимирует `font-size`/`margin-top` — это layout-свойства
    (триггерят reflow), а не `transform`: шапка должна реально уменьшиться в высоте, чтобы ряд
    описания/серий под кнопками поднялся на освободившееся место. Анимация включена только в
    режиме «Полные».

### Проверено по исходникам Lampa

- `SettingsApi.addParam` с `type: 'select'` и `values: {ключ: подпись}` — **подтверждено**
  (`src/interaction/settings/api.js` → `Params.select(name, values, default)`).
- `onChange(value)` вызывается для `select` и `trigger` — **подтверждено**
  (`params.js` → `bind()`: `Storage.set(name, value); if(onChange) onChange(value)`).
- Значение `trigger` хранится **строкой** `'true'` / `'false'`. Плагин это нормализует в `pref()`,
  так что и булево, и строковое значение читаются правильно.
- **`Lampa.Controller.listener.follow('toggle', fn)`** — **подтверждено живьём** на локальной
  Lampa 3.3.4: при спуске по карточке стрелкой вниз `e.name` идёт цепочкой `full_start` →
  `full_descr` → `items_line`, при подъёме — обратно.
- **События активностей**: `Activity.push` не шлёт событий для покидаемой активности; `backward()`
  шлёт `start` и `archive` той, к которой вернулись, и `destroy` — удалённой. Отсюда и приём: всё,
  что принадлежит экрану (герой, метки, мини-карта), снимается по `start` той активности, куда ушли.
- **`Lampa.Keypad.listener`** — источник событий клавиш; Keypad нормализует коды пультов Samsung
  orsay/LG и троттлит `keydown` до одного раза в 100 мс. Команды `toup`/`todown` (кнопки каналов) в
  Lampa 3.3.4 не реализует ни один контроллер — их и занимает быстрый прыжок по ряду.

### Места, где остаётся неуверенность в API Lampa

- **`Lampa.Platform.screen('tv')`** — подтверждено в `src/components/full.js`, но экспорт
  `Platform` в `window.Lampa` напрямую не проверен; есть фолбэк на `window.innerWidth > 480`.
- **`Lampa.TMDB.parseCountries` / `parsePG`** — подтверждены в исходниках `full/start.js`, но
  как поля `window.Lampa` не проверены; есть фолбэки на данные фильма.
- **`Lampa.Lang.add({key: {ru, en, uk}})`** — сигнатура общепринятая, но в скачанных исходниках
  не проверялась. При отказе используется внутренний словарь плагина.
- **Порядок событий.** Отрисовка навешана и на `type: 'build'` + `name: 'start'`, и на
  `type: 'complite'` — второй раз она идемпотентна.
- **Голосовой ввод в поиске подборок** — плагин зовёт штатную `Lampa.Input.edit` с раскладкой
  `search`, в которой у Lampa есть кнопка микрофона. Работает ли она на конкретном телевизоре,
  зависит от самой Lampa и устройства — своего API плагин не заводит.

---

## Тест-стенд

Реальной Lampa для проверки не требуется — в `harness/index.html` собрана минимальная эмуляция
API (`Template`, `Listener`, `Storage`, `Timeline`, `Api`, `Utils`, `TMDB`, `Lang`, `Platform`,
`SettingsApi`, `Controller`) плюс ключевые правила CSS из `start_new.scss` / `full_start.scss`,
и воспроизведена логика `Start.create()` из `src/components/full/start.js` строка в строку.

Запуск:

```powershell
cd "C:\Users\azark\Новая папка\lumen-card"
& "C:\Users\azark\AppData\Local\Programs\Python\Python312\python.exe" -m http.server 8765
# открыть http://localhost:8765/harness/index.html
```

(Можно открыть и файлом напрямую, но тогда jQuery с CDN должен быть доступен.)

В стенде:

- переключатель **Фильм / Сериал** («Дюна: Часть вторая» и «Fallout»);
- кнопки акцента и тумблеры шрифтов / «Продолжить»;
- **← / →** — эмуляция пульта: переставляет класс `.focus` между `.selector` в ряду кнопок;
- панель **Проверки** — автоматически проверяет, что все узлы и классы, нужные Lampa,
  присутствуют в отрендеренной карточке (и в сыром шаблоне — для узлов, которые движок удаляет
  по данным: `.full--tagline`, `.tag--time`);
- блок **JS ERRORS** — ловит `window.onerror` и исключения внутри слушателей.

`Lampa.Api.img()` в стенде намеренно возвращает пустую строку (TMDB может быть недоступен),
чтобы проверялся именно процедурный фон.

---

## Разработка

Плагин собирается из модулей `src/NN_*.js` (по числовому префиксу) в один файл
`dist/lumen_card.js`, который и коммитится в репозиторий и выкладывается на хостинг.

```powershell
cd "C:\Users\azark\Новая папка\lumen-card"

# сборка src/*.js -> dist/lumen_card.js (+ проверка синтаксиса node --check)
& "C:\Users\azark\AppData\Local\Programs\nodejs\node.exe" scripts/build.mjs

# ES5-линт собранного файла (парсер acorn на ecmaVersion: 5 + скан токенов на
# запрещённые ES2015+ API; ни одной находки быть не должно)
& "C:\Users\azark\AppData\Local\Programs\nodejs\node.exe" scripts/es5check.mjs dist/lumen_card.js

# юнит-тесты (более 2000 штук) без браузера
& "C:\Users\azark\AppData\Local\Programs\nodejs\node.exe" --test "test/*.test.mjs"

# только проверить, что dist/lumen_card.js не устарел относительно src/ (ничего не пишет)
& "C:\Users\azark\AppData\Local\Programs\nodejs\node.exe" scripts/build.mjs --check
```

`scripts/build.mjs` пишет во временный `dist/lumen_card.tmp.js`, гоняет на нём `node --check`
и только при успехе переименовывает в `dist/lumen_card.js` — синтаксическая ошибка в одном из
модулей никогда не перезатирает рабочую сборку. Баннер в начале файла не содержит даты
(`// Lumen Card for Lampa v<версия>`, версия — из `LC.VERSION` в `00_head.js`), поэтому сборка
воспроизводима: одинаковый `src/` всегда даёт побайтово одинаковый `dist/lumen_card.js`. Та же
сборка генерирует `manifest.json` из `src/42_manifest.js`, поэтому каталог в коде и на хостинге не
расходятся.

`dist` собирается без комментариев и без ведущих отступов (на ТВ-браузере это ~150 КБ лишнего
разбора) — исходники с комментариями живут в `src/`; границы комментариев даёт разбор acorn
(`scripts/lib/strip.mjs`), а не регулярки, поэтому строки с `/*` внутри CSS, data-URI с `//` и
регэкспы не страдают. Нумерация строк сохраняется: комментарий заменяется на столько же пустых
строк, сколько занимал, поэтому строка N модуля лежит на N-й строке после своего маркера
`/* ---- NN_имя.js ---- */` — на этом и держатся координаты `src/` в выводе `es5check`. Разбор идёт
с `ecmaVersion: 5`, так что ES2015+ синтаксис бракует уже сама сборка (с координатой в `src/`), не
дожидаясь `es5check`.

### Контракт модулей

Каждый `src/NN_*.js` — фрагмент тела одной общей IIFE, которую открывает `00_head.js` и закрывает
`99_tail.js`; глобал у плагина один — `window.lumen_card` (объект `LC`), `window.LC` не существует.
Из `00_head.js` в остальные модули неявно, через общую область видимости, пробрасываются только
`LC`, `PLUGIN` и `warn` — любое другое взаимодействие между модулями идёт только через `LC.*`
(`LC.util`, `LC.pref`, `LC.lang`, `LC.progress`, `LC.seasonsWord` и т.д.), а не через голые
идентификаторы. Модуль с чистой логикой оформлен как `LC.xxx = (function () { …; return {…}; })();` —
изолирующая обёртка с явным `return`, чтобы его внутренние функции не утекали в общую область
видимости. `scripts/build.mjs` перед сборкой сверяет верхнеуровневые имена всех модулей и падает
при повторе имени между файлами.

Строки интерфейса живут только в `LC.STRINGS` (`src/80_settings.js`) и всегда в трёх языках
(ru/en/uk) — литералов на русском в коде нет; тест проверяет, что у каждого пункта настроек и
каждого значения `select` есть строка во всех трёх языках. Собственные стили плагина лежат под
своими корнями и не задевают разметку Lampa.

Модули с чистой логикой проверяются тестами напрямую через `test/_load.mjs`; модули с рантаймом —
на фейковых `$`/`Lampa`/таймерах (`test/_fakedom.mjs`). Итог — больше 1400 тестов, которые гоняются
одной командой и обязаны быть зелёными перед каждым коммитом.
